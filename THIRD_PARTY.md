# Third-party components

本安装包不重新分发 Node.js、Java、.NET SDK、JDT LS、clangd 或 npm 依赖二进制；安装器从各自官方分发源下载固定版本。使用和再分发时应遵守各组件自己的许可证。

主要组件包括 Node.js、Pi Coding Agent、pi-lsp-extension、pi-claude-code-provider、Pyright、TypeScript Language Server、TypeScript、Eclipse JDT Language Server、.NET SDK、csharp-ls、clangd、Bubblewrap 和 Model Context Protocol SDK。
