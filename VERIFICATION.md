# Publication verification

## 2026-09-20 — Official headless primary CLI feature / 官方无头主代理入口

Added optional Codex, Claude Code and Antigravity transports through `scripts/headless-host.mjs`, a host-owned disabled-by-default configuration, explicit version/model/policy selection, stdin prompts with the packaged primary contract, normalized terminal results, process budgets/cancellation, and adapter/catalog parity checks. `Workflow.ps1` exposes HeadlessDoctor/HeadlessRun/HeadlessCompare; installation checks compare installed files and release builds run an isolated feature-package check. Pi providers, live role bindings and authentication adapters were not modified by this feature.

Validation: full Node suite **270/270** passed; targeted headless tests **11/11**, including actual Windows descendant cleanup, missing/changed versions, protocol failure, denial status, cancellation and output limits. `Test-Headless.ps1` passed an isolated copied runtime, inert defaults, wrapper invocation and altered-installation rejection. Portable/config/catalog/license checks, **35** source one-click checks and heap regression passed. A multiple-Node PATH issue found in the PowerShell wrapper was corrected and the wrapper checks rerun successfully. The restricted process environment initially could not confirm tree cleanup; the real-user Windows fixture verified it rather than weakening the failure status.

Live probes: Codex `codex-cli 0.153.0` and Claude Code `2.1.250 (Claude Code)` matched exact versions and required help flags. Authentication and model access were not tested. Antigravity was not found on PATH; its adapter has official-documentation and fixture evidence only. No model-backed CLI run, live-service deployment, CLI installation/update, account migration, Git publication or release was performed. Parity covers the feature modules/example/catalog, not the whole Pi installation or vendor-native settings. Source changes remain pending publication alongside the earlier code-graph work.

Governance: primary-local planning, pre-review, implementation, verification and post-review; no independent Pi reviewer execution is claimed. Existing memory statements on primary ownership, lower-agent binding and bilingual documentation were re-read against the updated primary contract before refreshing their source fingerprint. No automatic knowledge promotion or Git staging was performed.

中文：已加入三个官方 CLI 的可选无头主代理入口，默认禁用，包含版本/参数校验、统一结果、预算、取消、进程树清理及源码/治理目录一致性检查。完整 Node 回归 **270/270**、专项 **11/11**、隔离包检查、安装/配置/许可证检查、**35** 项单文件安装器源码检查与内存回归通过。本机仅验证 Codex/Claude 的版本和帮助；Antigravity 仅有官方协议和夹具证据。未调用真实模型、部署服务、发布或推送。本次审查由主代理本地完成，不宣称独立审查；既有知识经重新核实后更新来源指纹。

## 2026-09-20 — Local code graph deployment / 本地关系记忆部署

On subsequent explicit user authorization, deployment `code-graph-20260920-01` updated the installed Pi through a narrow gateway patch, graph modules, the pinned parser package, dependency metadata, notices and local on-demand policy references. Thirty-eight target files were hash-checked with before/after backups; 61 other installed runtime source files remained byte-identical. Host-specific authentication adapters were retained. The local global policy entry gained only the code-graph reference row.

The idle gateway was restarted under its existing maintenance mutex. Gateway health/readiness and Tunnel readiness passed. Advertised providers, authentication, LSP, roots, access, governance and project-memory policy compared equal before/after. Actual authenticated HTTP MCP verified graph discovery, all four read actions, the current 132-file graph, allowed-root denial, write-action denial and all five usable knowledge entries. Querying and installed-CLI no-op refresh preserved the project index. A separate tracked three-file JS/Python fixture exercised the installed WASM parser, dependency lookup, stale-query refusal, one-file incremental reparse and removal of the obsolete relationship. No model calls were made; this does not establish paid model availability or a fresh-machine installation.

用户授权后，本地 Pi 已更新并重启。更新前后保留文件备份，网关和 Tunnel 就绪；原有模型路由、认证、LSP、访问范围与治理配置一致。实际 MCP 和安装后解析器验证通过，既有五条知识仍有效。验证没有调用模型，未推送或发布；上文/下文实施阶段的“未部署”记录保留为历史状态。

Evidence: `.test/code-graph-deployment.json`, `.test/code-graph-live-check.json`; rollback backup: `C:/Users/asus/.local/state/pi-kether/deploy-backups/code-graph-20260920-01`. Primary performed local pre/post review; no independent reviewer verdict is claimed for this deployment.

## 2026-09-20 — Persistent code relationships (unreleased) / 代码关系持续记忆（未发布）

Added a deterministic JS/JSX, TS/TSX and Python syntax index in `.yhwh/code-graph/index.json`, read-only MCP queries, a fixed-path host refresh CLI and explicit bounded watching. Changed-file hashes reuse AST facts; all relationships are re-resolved for queries, including after deletion/rename/branch changes. Calls/base types remain unresolved mentions; reverse impact covers unique relative file imports only. No existing LSP isolation, runtime service, model route or credentials were changed.

Validation: final serial Node suite **259/259 passed**, no failures/skips, including **12 new graph tests**. Tests exercise real parsing, comment/string negatives, persistence across processes, no-op reuse, incremental edits/deletions/renames, newly tracked targets, branch/parser changes, ambiguity, syntax errors, pagination, stale rejection, locks/revision conflicts, concurrent refreshers and source drift, ignored/secret/binary/oversized/link paths, bounded watching and actual MCP root/read-only/lifecycle behavior without model calls. Existing heap regression passed. Portable self-test, workflow configuration, catalog synchronization and license inventory passed. The source one-click harness passed **35 checks**; no new full release archive or clean-machine installation was built/tested. All **74 checked local links** in the changed README pairs and graph guides resolved; language navigation remains present.

Real repository smoke: **132 tracked sources**, **690 named symbols**, **646 import records**, **10542 call/construction/base mentions**, zero parse errors, a 2219246-byte index. First refresh took approximately **3.2 seconds**; unchanged refresh approximately **2.6 seconds**, **0 reparsed / 132 reused**, unchanged index bytes and no write. These are single local measurements, not a latency guarantee. The source memory module's reverse import query returned eight file dependents through the gateway chain. Newly added untracked implementation files are intentionally outside this initial graph until an authorized Git add. Five human knowledge entries remain fresh; two changed source-backed entries were re-reviewed before updating their fingerprints.

Primary pre/post review only: automatic approval rejected external Claude review because the packet contained private repository details without specific disclosure authorization. No independent reviewer pass or runtime-attested stage chain is claimed. The parser uses pinned upstream WASM, with original notices for the package, runtime, bundled grammars and inherited C grammar; binary reproducibility was not independently established. No commit, push, release, live Pi deployment or host policy installation occurred.

已加入跨任务保存的代码关系索引、增量刷新、只读查询和显式限时监听。最终测试 259/259 通过，其中新增关系测试 12 项；原有堆回归、安装自检、配置、规则目录及许可检查通过。一键脚本仅运行源码夹具的 35 项检查，不代表新安装包或新机部署通过。真实仓库 132 个已跟踪源码均解析成功，首次约 3.2 秒、复用刷新约 2.6 秒。调用与基类保留为语法提及，不宣称完整语义关系图。独立外部审查被自动审批拒绝，采用本地前后审查；没有推送、发布或升级运行服务。

Evidence: `.test/code-graph-final-tests.txt`, `.test/code-graph-concurrency-tests.txt`, `.test/code-graph-memory-regression.txt`, `.test/code-graph-real-refresh.json`, `.test/code-graph-real-noop.json`, `.test/code-graph-real-impact.json`, `.test/code-graph-knowledge-review.json`, `.test/code-graph-oneclick-checks.txt`.

Verified on Windows on 2026-09-18 before the initial private GitHub publication.

- Clean dependency installation with lifecycle scripts disabled succeeded.
- Gateway automated suite: 181 passed, 0 failed, 0 skipped (`node --test --test-concurrency=1 tests/*.test.mjs`).
- Portable manifest, plugin metadata, policy reference targets and distribution-content checks passed.
- Installer configuration validation and no-write planning passed.
- The actual installer reference-copy block was exercised against an isolated temporary home: all nine references matched, the previous rule was backed up, and an unrelated reference was preserved.
- Source preparation excluded credentials, live configuration, runtime state, diagnostics, backups, dependency directories and Python caches. Credential-pattern findings were limited to synthetic redaction-test fixtures.
- The installed lifecycle gateway previously passed health/readiness and Luna/max and Sonnet/max no-tool heartbeats, including cleanup. These checks cover that host and those routes at that time.

This publication was checked by the primary agent locally. A fresh machine installation, a complete write-task governance chain, and the separately installed image workflow plugin were not validated by these packaging checks. Model access and authentication must be configured on the destination host.

The canonical distributable source in this repository is `payload/pi-dispatch`. Local machine source trees and an installed runtime are not automatically synchronized with it.

## One-click package 0.3.0

The primary agent performed local pre-change and post-change review; no independent model reviewer is claimed for this installer change.

- Windows PowerShell 5.1 executes the bootstrap and its test harness without requiring PowerShell 7 first.
- The harness exercises real ZIP extraction (including hidden-name plugin metadata and paths containing spaces), payload checksum rejection, traversal/duplicate/reserved-name rejection before destination creation, and no-write planning.
- Mocked process and WSL boundaries check existing-install protection, active-runtime rejection and distro ownership refusal/reuse. These are safety-branch tests, not real WSL installation evidence.
- Codex configuration checks cover new and existing `[features]` tables, preservation of unrelated tables, legacy root spelling, idempotence and explicit refusal of inline features before mutation.
- Release construction checks the generated self-contained installer, required policy/package files and exclusion of dependency trees and local state. Both README languages and their local navigation assets are distributed.
- The gateway suite passed 181/181 using the established `--test-concurrency=1` release baseline. An initial parallel run failed two HTTP-client fetch tests (179/181); it is not counted as a pass. Gateway implementation code was unchanged by this installer work.
- Both host and WSL Pi now consume the supplied lockfile. WSL provisioning sets its own Node/LSP PATH, creates `/usr/local/libexec` when absent, and verifies each LSP command separately. The Windows MCP configuration explicitly names the newly installed Pi entry point.
- PowerShell, Windows Node and the Ubuntu rootfs URLs and SHA256 values are pinned in `install/bootstrap-dependencies.json`, with publisher metadata links. Runtime download hashes are enforced during installation; a full runtime download/install was not performed during package validation.

Not performed: installation on a clean Windows VM, WSL enablement/reboot/import, UAC interaction, full dependency provisioning in the new distro, interactive OpenAI/Claude login, model heartbeat, or ChatGPT Tunnel setup. The current host's live Pi installation and WSL configuration were not replaced by these checks. Existing live-host heartbeats above do not validate this new installer.

## Multiple primary hosts 0.4.0

Verified locally on 2026-09-18. The primary agent performed local pre-change and post-change review. Automatic approval review rejected sending private design materials to the external Claude reviewer without explicit disclosure authorization, so independent model review was not performed or bypassed.

- The complete serial gateway suite passed 190/190, with no failures or skipped tests. Nine host-integration tests cover all eight profile formats, environment/path restrictions, refusal to overwrite existing exports, the allowlisted policy catalog, real in-memory MCP calls and a real stdio child connection. The HTTP-to-stdio proxy also retrieves the new primary contract.
- The generated Windows PowerShell 5.1 one-click installer passed 52 checks, including extraction of the host contract, catalog, exporter, connection checker and both integration-guide languages. Configuration validation and no-write planning reject invalid host selections. The checked-in catalog matches its canonical policy/skill sources.
- OpenCode 1.18.25 connected to the new gateway using isolated configuration/data/cache/state paths: its actual `mcp list` reported `yhwh connected`. Its `agent list` also reported `yhwh (primary)`. These checks did not call a model or establish prompt compliance.
- The actual Windows-side installer completed in an isolated target home with all seven non-Codex profiles selected, WSL and Tunnel provisioning disabled. Dependencies, host Pi entry, catalog and selected profiles passed installed checks. No `.codex` or `.agents` directory was created in that target home. This is not a clean-machine or fresh-WSL installation test.
- The release build passed plugin validation and distributable-content checks. The scanner now prunes excluded test/dependency/state directories before recursion instead of reading them and filtering afterward.
- Cherry Studio and Claude profiles follow published configuration interfaces. DSH rows follow the local native MCP client and agent-preset persona interfaces. OpenCode v1 and v2 have separate schemas. Generated profiles alone are not evidence of runtime support in every application/version.

Not performed: Cherry Studio, Claude Code/Desktop or DSH end-to-end host activation; OpenCode v2 runtime loading; new-host model heartbeats or a complete write-task governance chain; a clean Windows/WSL installation; or multi-host concurrency/load testing. The current live Pi installation and existing application profiles were not updated. The 0.4.0 source and release artifacts are local and have not been committed or pushed as part of this change.

## Common client profiles 0.5.0

Verified locally on 2026-09-18. Scope: add ten export adapters, primary instruction files, installer selection, bilingual documentation and a release package. Existing settings, running services, model routes and credentials were outside the change scope. The primary performed local pre-change review before edits and post-change review against the tests and documentation; no independent model review or linked Pi stage chain is claimed.

- Added Cursor, VS Code/Copilot, Windsurf Cascade, Cline, Roo Code, Gemini CLI, local Kiro, native Zed Agent, Continue IDE and LM Studio profiles: 18 host IDs including existing adapters. Official interface sources and scope are recorded in adapter metadata and both common-client guides.
- The complete serial gateway suite passed 195/195, with no failures or skipped tests. The expanded host-profile suite passed 14/14. For each of the ten new configuration shapes, the test extracts its actual command/arguments/environment, starts the real gateway through the MCP SDK and checks initialization, tool discovery and primary policy retrieval. This does not exercise the applications' own parsers or UI.
- Tests cover preserved executable argument boundaries and environment, absence of blanket tool approvals, full primary contract in native instruction files, always-on frontmatter where applicable, nested exports, refusal to overwrite existing output, and parity between all three installer host lists and the exporter.
- PowerShell configuration validation and read-only planning accepted all 18 IDs. The generated Windows PowerShell 5.1 installer passed 56 checks, including all ten new selections and inclusion of the new profile module and bilingual guide. Plugin validation, release exclusion checks, changed PowerShell syntax and ten maintained README files' language buttons/local links passed.
- No new-client application profile was written or activated. No model heartbeat, complete write-task governance run, client GUI/rule-loading test, clean Windows/WSL installation or concurrency/load test was performed. The earlier OpenCode evidence belongs to 0.4 and is not proof for the added applications. No commits or pushes were made for this change.


## Licensing and Claude Code assessment 0.5.1

Verified locally on 2026-09-18. Scope: Apache-2.0 for original YHWH work, preserved third-party notices, deterministic npm declaration inventory, bilingual documentation, Claude Code feasibility research, and packaging checks. Local pre-change review confirmed authorization and excluded service/authentication changes. Local post-change review checked source scope and the evidence below; no independent model review or linked Pi stage chain is claimed.

- Root and standalone plugin LICENSE/NOTICE, package metadata, lockfile root and portable manifest declare Apache-2.0 for original work. Upstream Pi/provider MIT texts and the Claude Code proprietary notice retain their original contents; source URLs and SHA256 values are recorded.
- The two pinned npm lockfiles provide 366 package-path entries, including optional/transitive dependencies, with zero missing license declarations. This is not a complete license-text, system-component or file-provenance audit. The LSP extension declares MIT but its complete upstream copyright/license notice was not found; that gap is explicitly recorded rather than fabricated.
- The license gate passed its valid isolated fixture and rejected four cases: changed original license bytes, wrong manifest license, stale inventory and changed standalone notices. All 74 checked local links in the updated README pairs, third-party documents and feasibility reports resolved.
- Release construction passed portable exclusion checks, configuration validation, host-workflow synchronization, plugin validation and license checks. The generated Windows PowerShell 5.1 installer passed 72 checks, including byte-identical licensing materials and feasibility documents after extraction.
- Runtime code and dependency versions did not change. The 195/195 gateway and 14/14 host-profile results above remain the earlier 0.5.0 baseline; this documentation/metadata/build change used Build-Release.ps1 -SkipTests and did not rerun those suites.
- Claude Code research compared official published guidance with credential-renewal, preparation, sandbox bootstrap and provider source. Direct primary-client use is technically feasible but was not exercised. The current subscription credential bridge is not approved by this assessment as a public default; an API/cloud adapter or specific upstream authorization remains outstanding.

No real credentials were read, no model calls or live service changes were made, and no clean-machine installation was performed. No commit, push, repository visibility change or GitHub Release was made for this licensing change. Earlier section statements about local/unpushed work describe their validation-time state, not the current GitHub history. License packaging success is not certification that every integration is ready for public distribution.


## API reviewer migration and LSP provenance 0.6.0

Verified locally on 2026-09-19. The user explicitly selected user-owned Anthropic API-key billing for the reviewer. Scope was repository sources, install artifacts, licensing evidence and documentation; the running host service, installed global policies and real credentials were excluded. Local pre-review established the selected route and fixed Pi capability before edits. Local post-review checked the changes and evidence below; independent model review is not claimed. Automatic approval rejected an initial broad edit; it was not executed. Work then proceeded through small reviewed source changes and fixture tests without bypassing the rejection.

- Reviewer route is now native `anthropic / claude-sonnet-5 / max`, with `access:none` and no tools. Subscription credential reading, CLI renewal, the custom Claude extension and CLI/provider installation dependencies were removed. All retained dependency versions are unchanged. The lockfile inventory now contains 356 package-path entries with no missing license declarations.
- API configuration uses a dedicated host file and hidden input. Windows PowerShell 5.1 tests passed invalid/subscription rejection before writes, exact fixture-key persistence, protected current-user-only ACL, key rotation, preservation after invalid replacement and temporary-file cleanup. An initial redundant ACL update requested an unavailable system privilege; the final script preserves the already restricted file ACL and verifies it after rename, and the tests passed.
- API validation rejects OAuth, subscription tokens, command-based keys, mixed credential payloads, mismatched models/providers, duplicate route arguments, tools and command-line API-key injection. Ambient Anthropic/Claude environment overrides are removed. The pinned Pi AuthStorage accepted the fixture API key in memory; its native Sonnet 5 catalog maps max effort. This is offline compatibility evidence, not remote model success.
- Complete serial gateway suite: 192/192 passed from the plugin working directory. Eight obsolete renewal tests were replaced by five API tests (previous baseline 195). An initial renamed-tool ordering assertion was fixed. A subsequent invocation from the repository root failed two working-directory-dependent editor tests; rerunning from the documented plugin directory passed all tests. No failing run is counted as successful.
- Generated Windows PowerShell 5.1 installer: 77 checks passed, including the new setup helper, API modules, LSP provenance/reference materials and byte-preserved licensing files. Configuration validation, workflow catalog checks, plugin validation and portable exclusions passed. Updated documentation had 82 valid local links.
- Python testing created an excluded bytecode cache containing the local source path; the source scanner now prunes bytecode caches consistently with the existing release exclusions. No credential detection pattern was relaxed.
- Both changed Bash files passed syntax checks with the installed Git Bash; an earlier guessed executable path was unavailable and is not counted as a check. The snapshot deny list now rejects the API credential filename including nested mixed-case paths; that platform-independent Python security test passed on Windows. Full Linux snapshot/runtime behavior was not rerun.
- The original pi-lsp-extension 1.3.0 npm tarball passed its published SHA512 integrity check. Evidence records source gitHead, hashes and all 53 archive entries. The original metadata declares MIT but the package and inspected repository history did not supply a full copyright notice. Standard SPDX MIT text is supplied explicitly as reference text without inventing attribution. Local patches write a distinct YHWH modification notice; applying the patch twice to the verified package was idempotent and preserved package metadata.
- `license-inventory.mjs --check --public` rejects the unresolved upstream notice. Public builds require a traceable upstream notice and matching digest; local preview packaging remains available. The upstream request is an unsent draft unless its own document records a submitted issue. No upstream response or permission is presumed.

Not performed: paid API heartbeat, real account/key/quota validation, new WSL provisioning, clean-machine install, live Claude Code UI integration, deployed service update, commit, push or GitHub Release. The API implementation removes the previous subscription bridge; it does not establish service endorsement or certify all third-party rights. Public release remains held for the LSP upstream notice.


## 0.7.0 controlled API transports — 2026-09-19

Scope: optional host-owned OpenCode Go, CommandCode, OpenRouter and custom HTTPS transports. Defaults stay openai-codex/Luna/max and Anthropic/Sonnet 5/max. No live service, global policy, real credentials, Git commit/push or external issue was changed by this phase.

Governance: primary-local scope compilation, classification, source scouting, plan and pre-review preceded mutations. Material clarification was not needed: the user authorized a controlled layer and the two named platforms. One local writer implemented and verified the change. Local post-review checked role preservation, endpoint/auth separation, config digest handling, FD3 bootstrap, release contents and documentation. No independent model reviewer was used; this is not an independent-review attestation.

Observed checks:
- Full gateway suite: **200/200 passed**, serial execution from payload/pi-dispatch. Includes 8 new controlled-provider cases. Evidence: local `.test/provider-gateway-tests.log` (excluded from release).
- Pinned Pi 0.84.4 SDK registered both providers and emitted the expected Responses and Messages requests into a mocked fetch: exact endpoint, Bearer fixture credential, model and `max`. **Zero external/model calls**. Reproduce with `node install/Test-ProviderSdk.mjs <pinned-pi-node_modules>`.
- Windows PowerShell 5.1 provider setup passed: hidden-input helper parsing, opaque fixture keys, separate references, current-user-only ACL, rotation retaining unrelated keys, invalid replacement retaining prior config/key, temporary-file cleanup.
- Explicit secret/traversal snapshot test passed, including provider-config.json and provider-credentials.json with nested uppercase variants. Full Linux snapshot/WSL integration was not rerun.
- Bash syntax passed for the sandbox and provisioning script. 58 local README/provider-guide links passed. Git diff whitespace check passed (line-ending normalization warnings only).
- Existing license inventory check passed with 356 lock entries and no missing metadata declarations. The separate upstream pi-lsp notice gap still blocks the public-release gate.

A full-suite first pass found one outdated assertion expecting OPENCODE_API_KEY inheritance. It now expects removal, matching host-selected FD3 credentials; the full rerun passed. An initial new child-process test had a newline escaping error in fixture code; corrected and rerun. No permission/capability check was weakened to pass.

The release builder runs provider setup tests and one-click extraction checks for the new configuration helpers, adapter and bilingual guide. Config and credential state files are excluded from snapshots/Git/release trees. API pricing is explicitly unknown, not the SDK placeholder zero. Config templates intentionally require operator confirmation of max capability before activation.

Unverified: real platform account/plan/model/max access, paid heartbeats, live tool calls, clean-machine installation, updated WSL execution, and running-service migration. CommandCode's example Sonnet 5 needs account catalog confirmation. Public docs establish endpoint shapes only. No automatic platform fallback, model substitution, lower-thinking fallback, custom headers, client impersonation or editor authorization was added.


## 0.8.0 API encryption — 2026-09-19

Authorized scope: add encryption for YHWH-managed API keys. Primary-local scouting, design/pre-review, implementation, verification and post-review; no independent model review. Real credentials, installed service/global policy, Git commits/pushes and public release were untouched. Existing OpenAI OAuth storage is outside this API-key change.

Implemented Windows DPAPI CurrentUser with slot-specific entropy; encrypted-only temporary and final files; restricted current-user ACL; atomic file replacement with no plaintext backup; explicit idempotent legacy migration. Runtime decrypts in the Windows helper and transfers selected credentials through private stdin framing and a kernel FD3 pipe. The WSL API branch no longer reads or creates API credential files. Legacy plaintext, mismatched config, corrupt ciphertext and failed decryption fail closed.

Observed verification:
- **202/202** serial gateway tests passed. Local evidence: `.test/encryption-gateway-tests.log` (not distributed).
- Real Windows PowerShell 5.1 DPAPI tests passed for roundtrip, tamper/slot-substitution rejection, migration, repeated migration, rejected replacement preservation, current-user ACL, rotation preserving other keys and temporary/backup cleanup. All keys were synthetic fixtures.
- Worker API test covers actual Windows DPAPI helper -> validated root pipe packet -> bootstrap credentials. Anthropic preflight covers explicit plaintext rejection followed by encrypted migration and successful local decryption. No remote model call occurred.
- Real WSL Ubuntu-24.04 / setpriv / Bubblewrap no-network probe passed: FD3 pipe delivered a fake credential object, stdin preserved the independent prompt, no credential file or service change. Reproduce explicitly with `node install/Test-ApiPipe.mjs <distro>`. This tests pipe isolation, not a paid Pi model execution or complete deployment.
- Bash syntax checks passed for sandbox/provisioner. Full release gate includes the new DPAPI tests and extraction checks for the encryption/migration helpers.

Compatibility fixes during verification: PS5.1 File.Replace requires an actual null string for the no-backup overload; use NullString.Value. Load the built-in Security module from PSHOME to avoid inherited PowerShell 7 module discovery. Source-loaded setup tests now load the shared module at script entry. The inline WSL fixture required --exec to prevent an extra shell expanding variables; production uses a script file.

Limits: no second-account/machine DPAPI test, live account heartbeat, full clean-machine installation or live upgrade. Runtime must briefly hold plaintext in memory; same-user/administrator/OS compromise is not prevented. Migration does not securely erase historic backups, freed disk blocks or third-party copies. Public release still has the separately documented upstream LSP notice hold.


## 0.9.0 development: multilspy deterministic semantics — 2026-09-19

Authorized scope: integrate microsoft/multilspy into repository sources and local install artifacts. Primary-local scouting and pre-review selected seven semantic operations, with the two structural operations and model-task extension tools retained. One local writer implemented the adapter. Local post-review checked fixed server selection, input scope, evidence status, cleanup, dependency provenance and the unchanged public-license hold; no independent model review is claimed. Existing services, credentials, published v0.8.1 assets and repository visibility were not changed.

Observed evidence:
- The serial gateway suite passed **202/202** after the semantic-engine metadata and bootstrap changes. Evidence: local `.test/multilspy-gateway-tests.log`, excluded from distribution.
- **17/17** Python protocol/lifecycle tests passed using the real multilspy protocol client and a fixture server. Cases include all seven operations, missing/stale/empty diagnostics, unsupported capabilities/encoding, UTF-16 positions, ambiguous queries, scope escape, denied server writes, protocol errors, timeouts and forced cleanup.
- Real provisioned Pyright passed Python diagnostics and definition; typescript-language-server with the isolated TypeScript 6.0.3 alias passed TypeScript diagnostics and hover. Diagnostics fixtures intentionally contain type errors: success means evidence was obtained, not that code was clean.
- Four real Bubblewrap checks passed: Python/TypeScript diagnostics through Node -> isolated Python -> multilspy -> server, plus retained overview/search through Tree-sitter. Network was isolated, workspace read-only, `/proc` empty, modelCalls zero, semantic cleanup successful and fixture source bytes unchanged. The runner uses temporary files and does not upgrade the installed service or read credentials.
- Initial TypeScript initialization failed because the pinned TypeScript 7 compiler has no tsserver.js. A separate 6.0.3 alias fixes the new probe while retaining the original compiler. Existing npm package entries were unchanged; one alias entry was added. Two test-harness setup errors (FD3 setup and executable permissions in the temporary overlay) were corrected before the successful complete rerun.
- Local packaging gates passed, including **108** generated one-click installer checks, workflow/configuration validation, API setup/DPAPI fixture tests, plugin validation and license inventory. An initial Windows file replacement failure in the unchanged encryption fixture passed on its standalone rerun and on the full packaging rerun; no encryption code or permission boundary changed.
- All 18 Python wheels were checked against official PyPI SHA-256 metadata; installation uses pinned versions, hashes, binary-only artifacts and an isolated pip configuration. Original Microsoft/OLSP notices and Python dependency notices are preserved. The npm inventory contains 357 package-path entries with no missing declared license metadata. The separate pi-lsp-extension upstream notice gap remains and the public-release gate must still reject it.

Reproduce adapter tests with `install/Test-Multilspy.py`; see the bilingual LSP component guide. Not verified: Java/C/C++/C# runtime queries, full project analysis, a new clean-machine install, complete deployed gateway request, cgroup resource exhaustion or service upgrade. No model call, commit, push or release was performed for this change.


## 0.9.0 development follow-up: official language adapters — 2026-09-19

The user selected multilspy's official language adapters. Primary-local scouting inspected the exact 0.0.15 wheel before mutation; it contains JediServer and TypeScriptLanguageServer but not the newer main-branch clangd adapter. Local pre-review selected Python symbol queries and JS/TS for official startup/handshake/shutdown, preserving explicit Pyright diagnostics/code-action routing and existing Java/C/C++/C# profiles. The pinned dependencies, single-file scope, credential/network isolation and public license hold remain. One local writer and local post-review were used; no independent model review is claimed.

- **20/20** protocol/lifecycle tests passed, including actual official adapter classes against fixture servers for five Python operations and seven TypeScript operations; read-only initialization and denied server writes; official failures without fallback; initialization encoding rejection; request timeout and forced shutdown cleanup.
- Real Python definition uses official JediServer with jedi-language-server 0.41.3. Real TypeScript diagnostics and hover use official TypeScriptLanguageServer. Python type diagnostics remain explicitly on Pyright. The first real Jedi launch used an unsupported module entry point and timed out with successful cleanup; the corrected fixed console entry point passed the complete rerun. No upstream source was patched or assertion disabled.
- **Six** real Bubblewrap checks passed: Python diagnostics/definition, TypeScript diagnostics/hover, and retained structural overview/search. The tests assert official route metadata, semantic cleanup, modelCalls zero, empty /proc, isolated network, read-only workspace and unchanged source bytes. Evidence: local `.test/official-multilspy-tests.log`.
- **202/202** gateway regression tests passed after exposing the official/controlled route matrix in capabilities. Evidence: local `.test/official-adapter-gateway-tests.log`.
- Official language adapters manage server lifecycle; query orchestration, evidence interpretation and safety boundaries remain YHWH-owned. Python uses the official factory; the TypeScript subclass overrides dependency setup to use locked preinstalled artifacts. The official initialization request is restricted to supported read-only capabilities and YHWH identity. Missing or incompatible official support returns a failure without an automatic alternate engine.

Limits: this is source/local-artifact integration, not a running-service upgrade or a complete clean-machine installation. Other language profiles have not been migrated or newly verified. JSX/TSX/JavaScript share the TypeScript adapter but did not receive separate real-server fixtures. Project-wide scope, paid model calls and independent review are not claimed. No commit, push, GitHub release, installed policy change or credential modification occurred.


## Live probe deployment — 2026-09-19

User explicitly authorized deployment to the actual running service. Local pre-review scoped this to the three probe scripts, the gateway LSP capability line, a dedicated multilspy virtual environment and the separate TypeScript 6.0.3 alias. Other installed gateway/model/authentication code was preserved, since the running service contains configuration that differs from the portable release. One local operator performed deployment and post-review; no independent model review is claimed.

Windows and WSL probe files were backed up under deployment ID `official-lsp-20260919-01`. The required Ubuntu Python venv package initially failed against stale package indexes; after refreshing them, venv support and its Python 3.12 security-package dependencies installed successfully. The 18 locked wheels were then installed offline with SHA-256 enforcement and passed pip check. The TypeScript archive passed the lockfile SHA-512 check. Hardened WSL has no Windows drive mounts, so artifacts were transferred through stdin without enabling host mounts.

The gateway was confirmed idle before stopping its exact process; the existing hidden runtime launcher restarted the gateway and verified the Tunnel. Live capability output identifies multilspy 0.0.15 and the official/controlled adapter matrix. Model providers and authentication capability objects match the pre-deployment values.

Five real MCP gateway requests passed: Python diagnostics detected the intentional type error via controlled Pyright; Python definition resolved the declaration via official Jedi; TypeScript diagnostics found the intentional type error and hover returned the expected number type via the official TypeScript adapter; structural search returned its expected match via Tree-sitter. Every request used executionMode direct, modelCalls 0, no unexpected tools and successful outer sandbox cleanup; semantic calls also reported successful language-server cleanup. Both diagnostic calls published actual diagnostics. Detailed local evidence is excluded at `.test/live-official-lsp-results.json`.

This establishes the deployed probe path for the checked operations, not all languages, project-wide analysis, a model heartbeat, clean-machine installation or reboot persistence. No Git commit, push, release, model call, credential migration or global policy change was performed. Earlier sections describe their original validation-time state; the previously built portable artifacts remain historical snapshots.

## Diagnostic completeness and concurrent cleanup repair — 2026-09-19

A subsequent 42-call audit found that protocol success was insufficient: six of 19 TypeScript diagnostic calls returned empty data for intentionally invalid input. The wrapper accepted the first matching publishDiagnostics notification, while the installed server publishes diagnostic categories incrementally. JS/TS diagnostics and code actions now await three fixed read-only synchronous tsserver requests, validate each response, and expose explicit completion evidence. Missing capabilities, partial/error/malformed responses, absent or stale notifications, and timeouts fail closed. This does not establish new completeness guarantees for other languages.

Live verification also reproduced a concurrent WSL cleanup failure twice. An isolated experiment showed WSL inheriting a cwd inside another task's temporary drive mount, producing unmount exit 32 while the child remained alive; explicit `--cd /` avoided that cwd. Task, cleanup, orphan scan and sandbox probe launches now pin their native cwd without changing explicit workspace scope. No lazy unmount, skipped cleanup check, sandbox relaxation, or model fallback was added.

User-authorized deployment backed up and updated the Python probe in Windows and WSL, then narrowly patched four WSL argument lists in the installed Windows module and restarted the idle gateway through its existing hidden launcher. The installed module intentionally preserves older model/authentication code instead of replacing it with the portable source. Gateway and Tunnel were ready afterward; provider and role-model capability objects were unchanged. Deployment IDs are `ts-diagnostics-completion-20260919-01` and `ts-diagnostics-wsl-cwd-20260919-01`.

Final targeted evidence:

- **26/26** protocol/lifecycle tests, including early-empty publication, complete clean results, all categories, malformed/failed responses, wrong URI/version, timeout, UTF-16 ranges and read-only code actions.
- **13** real temporary adapter checks: four semantic smoke checks and three rounds each of small invalid, 1,000-function invalid and clean TypeScript files. **Six** Bubblewrap checks passed; fixtures were unchanged. Saved at local `.test/ts-diagnostics-install-tests.log`.
- **6/6** focused Node tests for direct-LSP boundaries and native WSL cwd/cleanup.
- **38/38** final deployed-gateway requests: 37 through the local MCP client plus one through the active connector. All 30 intentionally invalid JS/TS diagnostic cases detected their expected error, four clean diagnostic cases returned empty completed results, and Python diagnostics, TS hover/code actions and structural search passed. All calls reported zero model use and successful sandbox cleanup; semantic calls also confirmed server cleanup. Two four-request concurrent batches completed in approximately 9.29 and 8.56 seconds, without the earlier unmount failures. Small/large TS diagnostic internal medians were 3.594/3.864 seconds (eight serial samples each).

The broad suite is **not reported as fully green**: the initial restricted run was 201/204 (two DPAPI fixture errors and one editor fixture connection error); the host-context serial run was 200/204, with four `fetch failed` results in local HTTP fixtures. Isolating that test file gave 31/32 with a different HTTP fixture failure. These were retained without weakening assertions or expanding the production patch to unrelated HTTP code. Logs are `.test/ts-diagnostics-gateway-tests*.log` and `.test/ts-diagnostics-gateway-isolated.log`. Independent Claude review was rejected by automatic approval because its packet contained private project information for an external provider; the primary performed local pre/post review, with no independent approval claim. No commit, push or release was made. Samples do not establish long-running load, every language, full-project analysis or reboot persistence.

## HTTP fixture port repair — 2026-09-19

The standard Node suite is now **209/209 passing in both default parallel and serial runs**, superseding the previous broad-suite failure status for that suite. Five new fixture tests cover Fetch port blocking, real loopback HTTP exchange, occupied-port recovery, bounded bind failure and invalid input rejection. Targeted gateway/lifecycle/proxy/fixture tests passed 51/51. A pre-change forced ephemeral-port allocation to 6667 reproduced `fetch failed` with nested cause `bad port`; repeating the same injection after repair passed all 46 gateway/lifecycle/proxy tests.

The host's TCP dynamic allocation range was observed as 1024–15000, which overlaps [WHATWG Fetch blocked ports](https://fetch.spec.whatwg.org/#port-blocking). Previously `listen(0)` could therefore bind a valid TCP port that the MCP client's Fetch implementation refused before sending HTTP. Earlier logs omitted nested causes, so not every historical failure can be individually attributed. The new test-only helper binds loopback ports in 49152–65535 and retains the bound socket for the fixture lifetime. Only EADDRINUSE/EACCES bind failures are retried, with a fixed budget; HTTP/business requests are never retried. Bind failures are propagated rather than treating Express's error callback as successful listening. Lifecycle fixture connection setup now sits inside its cleanup guard.

Gateway, lifecycle, stdio proxy and the separate memory harness share the helper. Production gateway code, runtime settings, Windows dynamic-port configuration and business assertions were not changed. Local primary-agent pre/post review was used; no independent review or deployment is claimed. Evidence: `.test/http-fixture-targeted.log`, `.test/http-fixture-forced-6667.log`, `.test/http-fixture-forced-after.log`, `.test/http-fixture-full-parallel.log` and `.test/http-fixture-full-serial.log`.

The optional `test:memory` command is **not included in the 209-test result and did not pass**. Its existing task/result fixture still uses an obsolete contract and fails the large-output assertion. A temporary contract-migration experiment progressed into processing a 256 KiB single-line result, consumed substantial CPU and ended with the MCP 60-second request timeout. That migration was reverted rather than changing memory limits, payload sizes or production processing to force a pass. Its HTTP listener migration remains; logs `.test/http-fixture-memory.log` and `.test/http-fixture-memory-fixed.log` preserve the two observations. This is a separate follow-up, not evidence that the repaired HTTP port selection failed. No commit, push, release or system configuration change was made.

## Large-result processing and memory regression repair — 2026-09-19

The prior memory-test failure is superseded by a passing run after a scoped production fix and fixture migration. The URL-credential redaction regex restarted a variable-length scheme match at each letter of a long non-URL string. Measured redaction times for 8/16/32 KiB inputs were approximately 56/218/872 ms. A scheme-run boundary now prevents those overlapping retries while retaining numeric/punctuation prefixes and credential masking. The repaired 256 KiB plain-text check took approximately 1.9 ms. Compact output is still redacted before truncation; full output is not shortened. New tests cover URL credentials, prefixes, ordinary URLs and three large input shapes in a child process with a five-second deadline, so synchronous regex stalls cannot hang the test runner indefinitely.

The memory harness now uses the actual v2 Chesed contract and asserts that all 40 large tasks reach the dispatcher, pass role validation and return the complete original 256 KiB result. The run passed 25 warmups, 400 ordinary calls, 40 large results, 40 client cancellations and 40 abrupt disconnections. Large-result MCP round trips averaged 27.4 ms with a 45.0 ms maximum. Retained heap grew 2.04 MiB and RSS 16.68 MiB; active handles decreased by three. Existing limits remain unchanged: 20 MiB heap, 96 MiB RSS, eight handles and 4 MiB heap growth per 100 recent ordinary requests. This bounded synthetic test does not prove an absence of leaks under every workload or actual provider behavior.

The standard default-parallel Node suite passed **211/211**. An initial run passed 210/211 because a successful editor stdio cold start exhausted its 1.5-second fixture budget under parallel load. Successful fixture operations now have a separate ten-second budget; the negative timeout test explicitly retains 1.5 seconds and its original rejection checks. No production editor deadlines changed. `Build-Release.ps1` now also runs the memory harness unless the caller explicitly skips tests, closing the previous release-test coverage gap. Evidence: `.test/large-result-targeted.log`, `.test/large-result-memory.log`, `.test/large-result-full.log` and `.test/large-result-full-final.log`.

After confirming the deployed gateway was idle, the matching installed `audit-log.js` was backed up and updated, and the existing hidden launcher restarted the exact gateway process. Source and installed hashes match; Gateway and Tunnel returned ready with unchanged provider/authentication capability objects. A real connector TS diagnostic request then detected code 2322 with direct execution, zero model calls, explicit diagnostic completion, and successful server/sandbox cleanup. Deployment record: `.test/large-result-deployment.json`. Other installed model/authentication code remains intentionally different from the portable package; this is not a full runtime migration. One local primary performed pre/post review; no independent model review, Git commit, push or public release is claimed. Full clean-machine installation, reboot persistence, native client UIs and the upstream license hold remain separate acceptance boundaries.

## Governed Pi custom LSP adapter — 2026-09-19

The first-party Apache-2.0 Pi adapter registers seven `yhwh_lsp_*` tools for file-admitted WSL workers. It sends typed IPC requests through the Pi bootstrap to a trusted launcher-side broker. Each request snapshots the selected current task file, then runs the existing multilspy probe in a separate read-only, credential-free, network-isolated sandbox. The broker requires the parent task's finite CPU, memory and process limits; it does not consume a second gateway task slot. Directory-descriptor traversal rejects symlinks, special files, oversized files and files changed during copying. Requests are serialized, bounded and cancelled with the channel/task; snapshots are removed after completion. Editor IPC uses a separate message namespace.

Validation passed **29/29** focused tests and **217/217** default-parallel Node tests. The real installed Pi **0.84.4** SDK loaded all seven tools in an isolated WSL fixture; the fixture exercised real TypeScript servers through the actual IPC broker. Checks passed for clean diagnostics, an intentional error after a worker edit, hover, definition, references, symbols, completions, read-only code actions, cancellation, descriptor confinement, empty `/proc`, absence of a synthetic secret, blocked networking and read-only mounts. All probe results used zero model calls, semantic checks reported server cleanup, and temporary snapshots/cgroups were removed. A nested sandbox attempt initially failed because the model sandbox has an empty `/proc`; the implementation keeps that protection and uses the trusted launcher-side broker instead. Evidence: `.test/pi-lsp-adapter-node.log`, `.test/pi-lsp-adapter-full.log`, `.test/pi-lsp-adapter-live-final.log`.

Deployment `pi-lsp-adapter-20260919-01` backed up and narrowly patched the idle installed Windows gateway/dispatch and WSL launcher, plus the new adapter modules. Matching deployed hashes, ready Gateway/Tunnel, and unchanged provider/authentication capability objects were verified. The installed adapter bytes then passed the same real-SDK fixture via `Test-PiLspAdapter.py --installed`. A separate post-deployment connector diagnostic request still detected TS code 2322 with explicit diagnostic completion and successful language-server/outer-sandbox cleanup. Evidence: `.test/pi-lsp-adapter-deployment.json`, `.test/pi-lsp-adapter-installed.log`, request `pi-lsp-adapter-postdeploy-20260919-01`.

These checks do not invoke a model or prove autonomous tool selection, every language, project-wide completeness, standalone Pi hosts, clean-machine installation or reboot persistence. The fixture loads the real SDK and deployed adapter modules but substitutes the model/auth bootstrap; the installed launcher is syntax-checked, not exercised with an actual provider request. Existing `lsp_*` and structural/rewrite compatibility tools still depend on pi-lsp-extension. Its license hold remains in effect. The primary performed local pre/post review; no independent model review, commit, push or public release is claimed.

## Task-local LSP session reuse — 2026-09-19

Adapter 1.1.0 accelerates consecutive governed Pi queries by retaining at most one isolated language server per task for an unchanged single-file snapshot. Its key includes normalized file path, SHA-256 content and server route. A changed key awaits old-server termination and creates a new snapshot; the implementation does not perform incremental document synchronization or reuse cached answers. Every query executes against the server and rereads the admitted source before returning, rejecting a changed hash. Idle expiry is 15 seconds. Cancellation, failure and task completion terminate the server and remove the snapshot; no session crosses tasks or enlarges file admission. Parent CPU/memory/process limits, no network/credentials and immutable probe mounts remain enforced.

Retained responses explicitly report `serverCleanup.ok:null` with state `retained-until-session-close`, plus hash/session generation/reuse evidence. This is not a claim of completed cleanup. Final broker cleanup is awaited and propagates failure to task exit. Cancellation can use whole-namespace termination. The primary's one-shot `lsp_request` contract remains unchanged and still awaits per-call cleanup.

Evidence:

- **30/30** Python protocol/lifecycle tests, including retained process identity, fresh queries, snapshot/path invalidation, failed-query shutdown, and all earlier diagnostic-completion checks. Final local review caught a boundary where a close message after the 64th query was incorrectly counted as a 65th query. The fix permits final close while still rejecting an extra query; an explicit protocol test covers both cases. The real SDK fixture passed again afterward (`.test/lsp-reuse-limit-final.log`).
- **223/223** default-parallel Node tests at the production-change checkpoint, then **225/225** in the release script's serial run, including malformed framing and failed final cleanup. The memory regression and **119/119** one-click checks also passed (`.test/lsp-reuse-build.log`).
- The real Pi 0.84.4 SDK fixture passed all seven TS tools, changed-file diagnostics with a new session generation, cancellation and isolation. Source and installed modules both passed. Sources: `.test/lsp-reuse-benchmark.log`, `.test/lsp-reuse-installed.log`.
- Two alternating cold/warm six-query sequences used diagnostics, hover, symbols, definition, references and diagnostics on identical TypeScript content, under the same finite task cgroup. Full semantic results matched. Source comparison: cold **30.893 s**, reuse **5.624 s** total across both rounds (**5.49x**). Installed comparison: cold **32.833 s**, reuse **5.656 s** (**5.80x**); individual six-call totals were 16.470/16.363 s cold versus 2.848/2.808 s reused. Timings include startup and final cleanup. The same fixture verified actual idle reclamation and a new generation on the next call. No model requests were made.

Deployment `lsp-reuse-20260919-01` backed up and updated only the broker, Python probe, first-party adapter version and capability metadata. Installed module hashes match. Gateway/Tunnel returned ready with identical provider/authentication capability objects. A post-deployment direct connector diagnostic still found TS error 2322 with explicit completion and successful server/sandbox cleanup (request `lsp-reuse-postdeploy-20260919-01`). Local deployment/verification evidence is under `.test/lsp-reuse-*`.

The 64-query close-boundary correction was subsequently deployed with a separate backup as `lsp-reuse-20260919-02`; ready state, matching module hashes and unchanged provider/authentication routes were rechecked. The final local installer is repacked after this correction and documentation update, reusing the passing Node/memory evidence above rather than repeating those unchanged suites.

The primary performed local pre/post review without an independent model review. Initial Windows-only transport fixtures required a platform-neutral injected file reader because production descriptor confinement uses Linux `/proc`; the real WSL confinement checks were retained and passed. These sample speedups do not establish other-language performance, large-project behavior, actual model-task wall time, sustained memory behavior, clean-machine installation or reboot persistence. Editing the file still incurs a cold start. No commit, push, public release, authentication migration or change to the legacy license hold was made.

## Python backend reuse — 2026-09-19

Adapter 1.2.0 retains up to two isolated backends for one unchanged Python file within a Pi task: Pyright for diagnostics/code actions and Jedi for the remaining semantic queries. The previous single-slot policy restarted a server on every switch between these routes. Other languages still use one slot. Both slots share monotonically increasing generations; changing the path or content closes the whole pool before replacement. Responses are fresh LSP queries, not cached answers. Each backend retains its 15-second idle expiry. Query failure, cancellation and task completion await cleanup of the whole pool. Repeated close waits for the same cleanup; a failed cleanup cannot become a successful result. Single-file admission, no credentials/network, task request budgets and parent resource limits remain unchanged.

Validation completed before deployment: **229/229** serial Node tests, memory regression, **30/30** Python protocol tests and real Pi SDK checks. New tests cover dual-route reuse, edit invalidation of both slots, cleanup of the other slot on error/cancellation and comparison with the old single-slot policy. Existing TS and malformed-response tests remain passing. Evidence: `.test/python-reuse-full-node.log`, `.test/python-reuse-memory.log`, `.test/python-reuse-live.log`.

Two rounds each alternate eight Python diagnostic/semantic queries, in opposite policy orders. The fixture asserts complete semantic data equality, correct reuse, no more than two snapshots and successful final/idle cleanup. Timings include startup and final cleanup. Source comparison totaled **25.408 s single-slot / 5.378 s dual-backend (4.72x)**. After deployment, the same comparison using installed modules totaled **24.848 s / 5.200 s (4.78x)**. Individual installed eight-call totals were 12.814/12.034 s versus 2.659/2.541 s; calls after both backends started took 2–11 ms in this fixture. All probes report zero model calls.

Installed sampled peak cgroup memory reached **218,415,104 bytes (208.3 MiB)** under the single-slot policy and **297,603,072 bytes (283.8 MiB)** with two backends. These are whole-test-cgroup measurements, including Node, servers and file cache; they are not isolated server RSS or model-task memory. The fixture kept its 3 GiB memory, one-CPU and 128-process limits. Retaining two backends consumes more memory; the optimization does not increase runtime resource limits. Source and installed runs separately verified actual idle expiry and Pi SDK Python reuse plus updated-file diagnostics. Evidence: `.test/python-reuse-installed.log`.

Deployment `python-reuse-20260919-01` backed up and patched only the broker and adapter capability/version metadata (the unchanged Python probe was hash-verified too). Windows and WSL module hashes match the staged files. Gateway/Tunnel returned ready with adapter 1.2.0, `pythonBackendSlots:2`, zero active/queued tasks, and unchanged provider/authentication capability objects. Local deployment evidence: `.test/python-reuse-deployment.json`. README and adapter documentation were updated in both languages. Local packaging reuses the passing Node/memory checks above; packaging validation and final artifact hashes are recorded separately in `.test/python-reuse-build.log` and `.test/python-reuse-report.json`.

The primary performed local pre-review and post-review; no independent model review is claimed. Installed SDK testing substitutes only the temporary fixture's model/auth bootstrap and invokes no provider. Results do not establish full model-task acceleration, large Python projects, all languages, long-duration memory use, clean-machine installation or reboot persistence. File edits still require fresh servers. No commit, push, public release, authentication migration or change to the legacy license hold was made.

## C/C++ exit-aware cleanup — 2026-09-19

Initial runtime inspection found `/usr/bin/clangd` absent despite the existing provisioner declaring the Ubuntu `clangd` package. Installed that dependency from the configured Ubuntu repositories (clangd 18.1.3). This restores an unavailable backend; it is separate from the measured optimization. Existing apt license/copyright notices remain installed with the system packages. No third-party binary was added to the portable ZIP.

Adapter 1.3.0 adds a C/C++-only transport cleanup override. Pinned multilspy 0.0.15 normally sleeps 0.5 seconds after closing server pipes. The new handler cancels transport readers through the existing lifecycle, retains process-group termination, drains remaining stdout/stderr and awaits actual process exit within two seconds. Missing exit evidence or timeout fails cleanup; the outer task isolation still provides final namespace/cgroup cleanup. It does not skip waiting, cache results, alter diagnostics/indexing flags or change other languages' cleanup behavior. Initial thread/background-index tuning produced no consistent C++ speedup and was not deployed. The optimization helps final cleanup, edit-triggered replacement and direct one-shot probes; already-reused semantic queries were fast before this change.

Validation:

- **32/32** Python protocol tests, including real large-pipe cleanup and rejection of missing process-exit evidence; **229/229** serial Node regression tests and the memory regression passed. Evidence: `.test/cpp-cleanup-source.log`, `.test/cpp-full-node.log`, `.test/cpp-memory.log`.
- Real Pi 0.84.4 SDK checks passed TS's seven tools, cancellation, Python dual reuse, and C/C++ seven-tool reuse plus edit invalidation and disappearance of the intentional diagnostic. Source and installed module runs both passed with zero model requests. These fixtures substitute the temporary model/auth bootstrap; they do not exercise an actual model task.
- Two alternating rounds per language each perform eight calls covering all seven tools. Old and new cleanup use identical clangd commands, inputs, immutable snapshots and finite resource limits. Full semantic data matched, reuse and final snapshot removal were asserted. Source totals: C **1.876 / 0.629 s (2.98x)**; C++ **1.627 / 0.616 s (2.64x)**. Installed totals: C **2.188 / 0.775 s (2.82x)**; C++ **1.754 / 0.775 s (2.26x)**. These totals include startup and final cleanup. First-run startup/cache variation contributes to the overall ratios; it is not a compiler-speed claim.
- Installed cleanup times decreased from **543–601 ms to 40–87 ms**. Peak whole-test-cgroup memory stayed about **315 MiB** (baseline/new maxima 330,772,480 / 330,731,520 bytes). Sampled task counts, including threads, peaked at 16 for both. No resource limits changed; memory includes test processes and file cache, not isolated server RSS. Evidence: `.test/cpp-cleanup-installed.log`.

Deployment `cpp-cleanup-20260919-01` backed up and updated the Python probe plus adapter version/capability metadata; the unchanged broker was hash-verified too. Windows/WSL deployed module hashes matched. Gateway/Tunnel returned ready at adapter 1.3.0 with `clangdCleanup:await-exit`, zero active/queued work and unchanged authentication/provider capability objects. README and adapter documentation were updated in both languages. Local package validation and source/archive comparisons are recorded in `.test/cpp-build.log` and `.test/cpp-report.json`; packaging reuses the already-passing Node/memory suites.

Local pre/post review was performed by the primary; no independent model review is claimed. These C/C++ fixtures are single-file examples, not header-heavy projects, project compilation, cross-file completeness, sustained memory or reboot-persistence evidence. No commit, push, public release or legacy-license-hold change was made.

## C# runtime compatibility and cleanup — 2026-09-19

Adapter 1.4.0 restores the C# runtime and reduces cleanup delays. Inspection found the declared .NET/csharp-ls dependencies absent. Installed the manifest-pinned .NET SDK 10.0.400 and csharp-ls 0.26.0. The old empty `/proc` prevented CoreCLR initialization; csharp-ls also requires a project for document analysis. C# probes now receive a read-only `/proc` inside their private PID namespace and a first-party minimal .NET 10 project containing only the admitted source. Intermediate/output paths are under `/tmp`; no user project, NuGet restore, network or credentials are admitted. Other languages and model tasks retain empty `/proc`. Fixed .NET settings disable telemetry, diagnostic ports and MSBuild reuse and cap the managed heap at 512 MiB per process, within the existing task resource limits.

C# reuses the existing one-file task-local session and the C/C++ exit-aware cleanup handler. Early server exit during requests or diagnostic publication immediately reports unavailable instead of waiting for the query deadline. The single-file project is a compatibility profile, not a reconstruction of the user's target framework or dependency graph.

Validation:

- **34/34** Python protocol tests passed, including early exits during initialization and after document opening. Source and installed real Pi 0.84.4 SDK fixtures passed seven C# tools, session reuse and edit invalidation with disappearance of CS0029, alongside TS, Python and C/C++ checks. Zero model calls. Evidence: `.test/csharp-source-final.log`, `.test/csharp-installed.log`.
- Isolation checks passed for the C# private PID view, read-only source and proc mounts, cleared synthetic secret, inaccessible host process/root paths and blocked networking. Existing descriptor confinement and empty-proc checks for other languages also passed. The direct launcher restricts private proc to credential-free single-file C# probes.
- **232/232** serial Node regression tests and the memory regression passed in the host user's environment. The initial sandboxed run exposed two fixture changes (copying the new helper module and distinguishing private C# proc from model proc) and two Windows DPAPI sandbox restrictions. The fixtures were corrected and the entire suite rerun successfully; production credential behavior was unchanged. Evidence: `.test/csharp-full-node-final.log`, `.test/csharp-memory.log`.
- Two alternating eight-call rounds compare old and exit-aware cleanup, with the same new compatibility profile in both groups. All semantic results matched after validating and excluding only random completion resolve UUIDs. Source cleanup decreased from **625–738 ms to 109–128 ms**; installed cleanup decreased from **617–638 ms to 156–198 ms**. Source total time was **26.089 / 25.429 s (1.03x)**; installed total was **28.410 / 33.329 s (0.85x)**. Whole-sequence speedup was therefore not consistently demonstrated. Installed cold startup varied from **9.849 to 14.087 s**; restoring availability and reducing cleanup waits are the established benefits. The installed benchmark overlapped a host regression run, so these small samples do not establish a throughput regression either. Peak whole-test-cgroup memory was **420,761,600 / 416,882,688 bytes** for baseline/new; task counts peaked at **38 / 37**, including threads.

Deployment `csharp-20260919-01` backed up and narrowly updated the Windows and WSL broker, probe, new project helper, adapter metadata and the direct launcher's C# proc branch. The installed launcher and gateway were patched in place to preserve their host-specific auth code. Deployed hashes matched, Gateway/Tunnel returned ready at adapter 1.4.0 with no active/queued work, and provider/authentication capability objects remained identical. A real post-deployment direct connector request `csharp-postdeploy-20260919-01` detected CS0029 with `executionMode:direct`, `modelCalls:0`, `diagnosticsPublished:true` and successful server/outer-sandbox cleanup. Evidence: `.test/csharp-deployment.json`, `.test/csharp-postdeploy.json`.

Both README languages and adapter documentation describe the C# isolation exception and limitations. Local installer packaging reuses the passing Node/memory checks; one-click archive checks and final artifact/source hashes are recorded in `.test/csharp-build.log` and `.test/csharp-report.json`. No third-party binaries are added to the portable ZIP.

The primary performed local pre/post review; no independent model review is claimed. SDK fixtures replace only their temporary model/auth bootstrap. These results do not establish autonomous model-tool selection, complete Unity assemblies, project-wide compilation, large-project performance, clean-machine installation or reboot persistence. Cold starts remain costly. No commit, push, public release, auth migration or legacy-license-hold change was made.

## Java runtime and short-task JIT — 2026-09-20

Adapter 1.5.0 restores Java probing and tunes JDT LS for bounded tasks. Inspection found Java and JDT LS absent despite the provisioner's existing dependency declarations. Installed Ubuntu's OpenJDK 21 JRE (21.0.12) and pinned JDT LS 1.60.0-202606262232; the archive matched the official SHA-256 `e94c303d8198f977930803582738771fd18c52c5492878410bf222b1aa81ef1d`. An initial Windows text-mode pipe converted shell newlines to CRLF and failed; the corrected installation used raw UTF-8 bytes, and runtime versions were verified afterward. No server binary was added to the portable ZIP.

The new first-party launcher uses the fixed Java binary and library paths, avoiding missing `/etc/alternatives` links and `/proc`-dependent library lookup. Only the JDK security configuration directory is mounted read-only; management credentials are excluded. `/proc` remains empty, source mounts remain read-only, and network/credential isolation is unchanged. JDT LS configuration copies, indexes and cache live in the probe's disposable `/tmp`. Fixed initialization disables Maven/Gradle import, automatic builds and additional referenced libraries; only the admitted file and installed standard library are available. Non-project/off-classpath diagnostic codes 16/32 explicitly return unavailable rather than passing as semantic checks.

The JVM profile sets one processor, Serial GC, a 64 MiB initial heap, 512 MiB maximum heap and tier-1 JIT. It targets short task-local server reuse, not arbitrary persistent JVM services. Java shares the existing exit-aware request and cleanup lifecycle, including immediate failure on early process exit. Task resource ceilings, one-file session keys, edit invalidation, 15-second idle expiry, request limits and final cleanup remain enforced.

Validation before deployment: **37/37** Python protocol tests, **232/232** serial Node tests and the memory regression passed. New protocol cases cover immediate Java exit, rejection of syntax-only/non-project diagnostic evidence and fixed initialization settings. Real Pi 0.84.4 SDK tests passed Java's seven tools, actual hover/definition/reference content, session reuse and disappearance of the intentional type error after editing. Existing TS, Python, C/C++ and C# SDK fixtures also passed. Isolation checks verified read-only JDK security/source mounts, absent management directory, empty `/proc`, cleared synthetic secrets and no network. All probes used zero model calls. Evidence: `.test/java-source-sdk.log`, `.test/java-node.log`, `.test/java-memory.log`.

The performance fixture alternates two default-tiered and two tier-1 runs. Both include identical compatibility fixes, heap settings, single processor and exit-aware cleanup; only JIT tiering differs. Every run uses eight calls covering seven tools, checks complete semantic data equality without dropping fields, detects the type mismatch, verifies session reuse and awaits final cleanup. Source totals were **45.506 s baseline / 26.202 s tuned (1.74x)**. Source tuned cold diagnostics took **6.651–7.263 s**, first references **5.307–5.430 s**, and subsequent small queries generally milliseconds. Whole-test-cgroup peak memory was **1,061,007,360 / 997,470,208 bytes**, with task counts including threads **42 / 41**. These memory measurements include the fixture, JVM and file cache, not just Java heap or RSS. The benchmark passed before a later SDK fixture hit a root-owned filename collision; giving the SDK fixture its own file resolved that test-only issue, and the full source SDK suite passed again. Evidence: `.test/java-source.log`, `.test/java-source-sdk.log`.

Deployment `java-20260920-01` backed up and narrowly patched the installed Windows/WSL broker, Python probe, Java launcher, direct-launcher Java mount branch and adapter metadata. The installed gateway and launcher retain host-specific authentication code. Deployed hashes matched; Gateway/Tunnel returned ready with adapter 1.5.0 and unchanged provider/authentication capability objects. The real direct connector request `java-postdeploy-20260920-01` detected Java return-type error 16777235 in **8.472 s**, with `executionMode:direct`, `modelCalls:0`, `diagnosticsPublished:true` and successful server/outer-sandbox cleanup. Evidence: `.test/java-deployment.json`, `.test/java-postdeploy.json`.

The installed-runtime comparison also preserved complete semantic results: **47.867 s baseline / 26.892 s tuned (1.78x)** across two eight-call rounds. Tuned individual totals were **13.525 / 13.367 s**, cold diagnostics **6.670–7.090 s**, and first references **5.611–5.824 s**. Baseline/new peak whole-test-cgroup memory was **1,043,116,032 / 1,018,916,864 bytes**, task counts including threads **42 / 41**. The installed SDK suite subsequently passed all existing language fixtures plus Java seven-tool reuse and edit invalidation. This run did not overlap the Node regression or package build. Evidence: `.test/java-installed.log`.

README and adapter documentation were updated in both languages. The local installer includes the Java launcher and benchmark; its packaging checks reuse the passing Node/memory runs. One-click checks, source/archive comparisons and artifact hashes are recorded in `.test/java-build.log` and `.test/java-report.json`.

The primary performed local pre/post review, with no independent model review. SDK fixtures replace their temporary model/auth bootstrap and do not prove autonomous model tool selection. These are single-file/standard-library checks, not Maven/Gradle, Android or multi-module build validation, long-duration JVM throughput, clean-machine installation or reboot persistence. Full JIT may perform differently in long-lived or larger workloads. No commit, push, public release, auth migration or legacy-license-hold change was made.

## 2026-09-20 — Go / Rust 单文件适配（yhwh-pi-lsp 1.6.0）

- 主代理本地前审、实施及复核；无独立模型审查，全部验证 `modelCalls:0`。保留已有工作区修改，没有提交、推送或公开发布。
- 固定工具链：Go 1.27.1、gopls 0.23.0、Rust/rust-analyzer 1.98.1；官方归档固定 SHA-256，gopls 使用官方模块代理及校验库构建，保留上游许可证及构建材料。
- 修复实测问题：gopls 必须读取自身可执行文件，使用只读私有 PID 视图；固定版本的完整 pull 结果可能有空 `kind`，严格要求 `items`，不接受 `unchanged`。Rust 实测曾先发布空诊断再报告类型错误，现要求独立 rustc 元数据检查完成并清理后才能返回完整诊断。
- Node 回归 **234/234**；Python 协议 **42/42**；内存回归通过。首次受限宿主运行中的两项 DPAPI 用例因环境失败，重跑于真实用户环境通过；另修正新增夹具的错误码期望和隔离静态断言。没有降低断言或跳过测试。
- 源码与安装态 SDK 检查覆盖全部语言；Go/Rust 的七项工具、任务内复用、重复诊断、编辑失效及错误→干净结果均通过。另检验 Go `fmt` 标准库导入、Rust `E0382` 所有权错误和 UTF-16 位置；编译器超时、输出超限、异常退出及不完整证据均拒绝通过并确认进程回收。
- 正式直接网关在错误夹具检出 Go `IncompatibleAssign` 和 Rust `E0308`，均有 `diagnosticCompletion.complete:true`、`diagnosticsPublished:true`、`serverCleanup.ok:true`、外层 `cleanup.ok:true`，执行耗时分别 **3248 ms / 5404 ms**，不是热调用基准。
- 独立检查 Go 只读私有 PID 视图、无宿主进程/根目录可见性、无网络、无凭据和源文件只读；Rust 保持空 `/proc`。文件/会话/任务结束清理继续使用现有限额与机制。
- 本机更新采用哈希防漂移及回滚备份，仅替换相关探针和版本声明。部署 `go-rust-20260920-01`，原模型及认证路由比对一致。完整证据位于本机 `.test/go-rust-*.log/json`。
- 范围：单文件及可用标准库；Go 外部模块/cgo、多文件 Rust/Cargo 工程、构建脚本、过程宏均不属于本次验证。首次全新机器安装未执行，本次验证固定依赖实际安装与现有服务升级。公开发布许可门禁保持原样。

### English

Adapter 1.6.0 adds pinned Go/gopls and Rust/rust-analyzer support with completed diagnostic evidence. Local primary-agent review, 234 Node tests, 42 Python tests, memory checks, real source/installed SDK calls, edit invalidation, standard-library checks, Rust ownership errors and cleanup were verified with zero model calls. Two DPAPI checks initially failed under the restricted host environment and passed under the real user environment; new fixture error-code expectations and the isolation assertion were corrected without weakening checks. Direct deployed requests detected the expected Go/Rust type errors in 3248/5404 ms with both cleanup layers confirmed. These are small cold-request samples, not a performance guarantee. Installed provider/authentication routes were preserved. Scope remains single-file/standard-library analysis, not full module/Cargo builds, build scripts, proc macros or a clean-machine installation. No commit, push or public release was performed; existing release gates remain.

## 2026-09-20 — Project knowledge and Git review / 项目知识与 Git 差异管理

Added a read-only project_memory MCP/CLI interface over project-local .yhwh/memory Markdown records. It provides lifecycle-aware keyword search, source fingerprints, staged/unstaged Git patches and explicit untracked additions. The primary edits and accepts entries using existing host permissions; there is no new write endpoint, automatic promotion, Git mutation or background watcher. Five reviewed bilingual YHWH entries cover architecture, LSP evidence boundaries, knowledge/documentation conventions, licensing boundaries and verification/packaging. All five current source checks passed.

新增项目级 Markdown 长期知识与只读 MCP/CLI；按草稿、确认和废弃状态管理，检测声明的来源变化，并区分已暂存、未暂存和未跟踪的知识差异。写入和确认仍由主代理按宿主权限执行，不自动提交。已初始化五条双语知识，当前来源检查均通过。

Validation: 13/13 new deterministic tests passed, including persisted reload in another process, Chinese/English lookup, stale-source exclusion, CRLF normalization, missing/staged-removed/changed-branch sources, index preservation, ignored/unsafe/binary/oversized sources, junction/hardlink rejection, malformed metadata, bounds, literal Unicode paths, disabled external diff/textconv/fsmonitor, common credential redaction and actual MCP transport/root/lifecycle checks. The final full Node suite passed 247/247 with no skipped tests in the real Windows user environment. The first full run had one expected tool-list mismatch after adding project_memory; the exact allowlist was updated, then the entire suite passed. Portable self-test, workflow catalog consistency, license inventory check and Git whitespace checks passed. Local evidence: .test/project-memory-full-suite-final.tap and .test/project-memory-initial-review.json.

Review boundary: the Pi pre-review attempt timed out after 301409 ms and returned no valid verdict. The primary completed local pre/post review; independent model review did not pass and is not claimed. The code has no model calls for memory operations. Fingerprint equality does not establish semantic correctness or undeclared dependency freshness. Git must already be installed on the runtime host. A live production-gateway upgrade, clean-machine installation, Git commit/push and release were not performed; published artifacts were not rebuilt. The source CLI is usable now, and the MCP integration was verified with a test gateway.


## 2026-09-20 — Knowledge deployment and 0.10.0 preparation / 知识工具部署与版本准备

After the separately authorized feature push (2d91798) and local upgrade, deployment project-memory-20260920-01 added the knowledge module, narrowly patched the installed gateway and synchronized the local on-demand policy. Backups and before/after file hashes were retained. Provider/authentication settings, LSP capabilities, allowed roots and access modes compared equal. The gateway and Tunnel became ready after restart; no model calls were made.

Actual authenticated HTTP MCP checks passed discovery, review of all five fresh entries, LSP keyword search, individual entry read, source snapshot, out-of-root denial and write-action denial. Repository knowledge had no pending Git changes after the feature push. Evidence: .test/project-memory-deployment.json and .test/project-memory-live-check.json. The source Node regression preceding that deployment passed 247/247 including 13 new knowledge tests; this does not prove paid model access or full clean-machine installation.

用户后续授权推送和本机升级后，已完成知识模块及规则入口部署。实际 MCP 验证通过：五条知识均有效，读取、检索、来源指纹、范围拒绝和写入拒绝正常；认证、模型路由、LSP 与访问范围不变，保留回滚备份。先前记录中的“未推送/未部署”是实施阶段状态。0.10.0 为随后独立授权的打包发布步骤；不会将该项目的知识条目复制到其他项目，也不改变上游 LSP 通知缺口或公开发布检查。

## 2026-09-20 — Workflow operations optimization / 工作流运行优化

Primary-agent local pre-review, implementation, verification and post-review; no independent Pi reviewer verdict is claimed. The accepted scope covers portable CLI acceptance, repeated-call overhead/progress, lifecycle containment, compatible host-plugin upgrades, change-impact candidates and CI. Antigravity's executable remains empty and disabled as requested. No commit, push, service deployment, credential migration or new release was performed; v0.11.0 published assets remain separate from development artifacts.

Final Node regression: **286/286 passed**, zero skipped, in the real Windows user environment (`.test/optimization-final-tests.txt`, 188725 ms). The suite includes help-cache invalidation and exact-version rechecks, sanitized progress, batch stop-on-failure, cancellation/timeouts, Job Object cleanup after normal child exit and abrupt Node-runner death, real executable/script identity checks, authenticated maintenance and graceful disposal, managed-file success/rollback/conflict paths, and read-only Git/graph/knowledge candidates. Earlier failures identified a wrong fixture tool name and nvm junction-path comparison; both were corrected without weakening assertions. Restricted-environment process cleanup tests were rerun under the real user account.

Heap regression passed with 400 ordinary requests, 40 large-output, 40 cancellation and 40 disconnect cases after 25 warmups. Heap growth was 2218880 bytes, RSS growth 24522752 bytes and active handles decreased by three. This is an isolated synthetic resource check, not a model/LSP performance benchmark.

The final real CLI echo checks used fresh empty Git fixtures, random exact-response tokens, unchanged official CLI authentication and native process-tree mode. Codex and Claude both passed (`.test/optimization-live-final.json`). Total times were 9866/5422 ms, preflight 617/952 ms and first response 8192/3931 ms. These single samples do not establish a speedup; the earlier 11620/6595 ms samples are not a controlled benchmark. Actual CLI permission enforcement, cancellation during model work and CLI-to-Pi MCP remain unverified; the CLI acceptance report deliberately remains `complete:false`. Antigravity real execution is unverified.

Portable/configuration/credential-fixture checks, isolated headless copy and drift checks, catalog consistency, license inventory (358 lockfile entries, no missing declarations), Codex plugin validation and **128 generated one-click checks** passed. Development packaging uses `.test/optimization-build` and does not replace `release/` assets. The CI YAML parsed successfully with pinned Actions, seven steps and read-only repository permissions; a clean-target installation plan passed. No GitHub CI run, clean-machine Windows/WSL installation or reboot test was performed. The existing public-release licensing gate is unchanged.

The repository graph refreshed six tracked files and reused 133, with no parse errors. A real read-only change-impact report identified two knowledge-review candidates and eleven test-file candidates (`.test/optimization-impact.json`). Existing knowledge entries and the Git index were not updated/promoted; the two affected entries remain needs-review. New untracked source files are explicitly unindexed until a separately authorized Git add. The graph is a bounded relative-import aid, not complete semantic dependency or test coverage proof.

Upgrade guarantees are limited to compatible managed host-plugin files: busy services, local drift, dependency changes and WSL-copy changes block replacement. Tests cover transactional failure paths and real Windows process identity; the production update wrapper has not upgraded the user's running service. Startup acceptance additionally checks the authenticated process identity, not only a healthy port. Cleanup failure blocks replacement. Crash recovery is manual via retained journal/lock; no automatic crash-recovery command is claimed. Local authentication/configuration and dependency directories are excluded from managed backups/replacement.

Automatic approval initially rejected a combined synchronization/test command because it might modify host workflow state. The script was inspected: its sole write is repository-local `payload/pi-dispatch/workflow/catalog.json`. Synchronization then completed under ordinary restricted permissions, separately from elevated fixture tests. No host policy or live service was changed.

### 简体中文

本次由主代理完成本地前审、实施、验证和复核，不宣称独立 Pi 审查通过。新增 CLI 回显验收、批次/进度及耗时证据、可选 Windows Job Object、受管插件升级/回滚、Git 变更影响候选与 Windows CI；Antigravity 路径按用户要求留空并禁用。最终 Node 回归 **286/286**、堆内存回归及 **128 项生成安装器检查**通过。修复了测试中的错误工具名与 nvm 链接路径误判，未降低断言。

Codex、Claude 在空白临时 Git 目录中的最终真实随机回显均通过，总耗时 9866/5422 ms；这是单次连通性证据，不是提速基准。真实 CLI 权限、模型工作期间取消及 CLI→Pi MCP 尚未验证，报告继续保留 complete:false；Antigravity、真实运行服务升级、全新机器安装、重启和远端 GitHub CI 也未验证。Job Object 的异常退出清理与升级故障路径由确定性夹具验证，不把它当作文件权限沙箱。

关系索引刷新 6 个已跟踪文件、复用 133 个且无解析错误；实际变更影响报告得到 2 条知识复核、11 个测试候选。未自动确认知识或暂存新文件，两条受影响知识保留 needs-review。升级只覆盖兼容的宿主插件文件，依赖/WSL 变化及本地冲突会阻止替换；崩溃恢复仍需人工核对保留的日志和锁。开发包位于 .test/optimization-build，既有 v0.11.0 附件未覆盖；未提交、推送、部署、迁移认证或发布。自动审批曾拦截合并命令，经确认同步脚本仅写仓库目录后，以受限权限单独完成，无宿主配置变更。

## 2026-09-20 — Upgrade lifecycle and live cancellation repairs / 升级生命周期与真实取消修复

Local primary-agent pre/post review; no independent model review is claimed. Preserved the existing uncommitted optimization work. Fixed maintenance acquisition failure incorrectly resuming another operation's maintenance state. The updater now checks service identity before shutdown, creates hash-verified backups and a prepared journal before stopping, and retains recovery evidence when admission cannot be restored. Actual process/HTTP integration testing then reproduced a service that disappeared after the updater exited despite a completed/rolled-back result. Detached hidden startup corrected both successful upgrade and failed-new-version rollback. The tests use the real gateway module with an injected fixture sandbox and no model calls; they do not upgrade the production Pi or prove WSL/Tunnel provisioning.

Added optional `HeadlessAccept -Live -CancelProbe` / `headless-acceptance.mjs <config> --live --cancel`. It first verifies echo, then starts a separate invocation and requests cancellation 1500 ms after entry-process launch. Preflight time is excluded. Early completion and fixture/setup failures cannot produce successful requested acceptance; failed/incomplete requested checks return nonzero. CLI signals propagate to active acceptance. Permission and CLI-to-Pi MCP fields remain separate and unverified.

Real checks: Claude native echo/cancellation passed (834 ms cancellation cleanup). Codex native echo passed but cancellation returned cleanup_unverified (956 ms), and this failure remains in `.test/repair-live-cli.json`. A separate explicit Job Object configuration passed Codex echo/cancellation, with 38 ms cleanup (`.test/repair-live-job.json`); this is not an automatic configuration fallback or a benchmark. Job Object cancellation now closes the launcher's non-inheritable kill-on-close handle instead of racing taskkill's descendant enumeration. A deterministic test cancels while a known descendant is alive and verifies its PID no longer exists. Local process-tree cancellation does not establish remote computation or billing cancellation.

Validation: **294/294 full Node regression** passed before the final focused Job Object change (`.test/repair-final-suite.txt`, 192056 ms). The final headless suite then passed **17/17**, including the new live-descendant cancellation fixture (`.test/repair-job-tests.txt`). Four new acceptance fixtures passed as part of the full regression. Three real service-process scenarios passed: success, failed-new-version rollback, and existing maintenance preservation (`.test/repair-upgrade-final.txt`). Heap/resource regression passed (`.test/repair-heap.json`). Isolated headless packaging/drift checks passed. The graph refreshed two tracked files, reused 137 and reported no parse errors; new untracked modules remain unindexed until authorized staging. No knowledge entry was automatically accepted.

Development packaging is confined to `.test/repair-build`; build results are recorded in `.test/repair-build.log`. Published `release/` assets, installed Pi, host authentication, global policies and Git staging were not changed. Antigravity remains disabled with an empty path. Production update, fresh-machine installation, restart persistence, live CLI permission enforcement, CLI-to-Pi MCP and remote GitHub CI remain unverified. Manual crash-journal reconciliation remains required; no automatic crash-recovery command was added.

### 简体中文

保留上一轮未提交的改动，由主代理本地前审、实施及复核。修复维护失败误解除他人维护状态的问题；停服前建立并校验备份日志，无法恢复接单时保留恢复证据。真实进程/HTTP 夹具进一步复现了升级器退出后服务消失的缺陷，已通过独立隐藏后台启动修复；成功升级、启动失败回滚和维护冲突三个完整场景均通过。夹具注入测试沙箱，未升级实际 Pi，不能据此证明 WSL/Tunnel 部署。

新增显式 `-Live -CancelProbe`：回显后另启进程，在入口启动 1.5 秒后取消；提前完成、夹具失败、清理无法确认均不算通过。Claude 原生取消通过，清理 834 ms。Codex 原生路径仍为 cleanup_unverified；显式 Job Object 路径通过回显与取消，清理 38 ms。未自动改写用户配置；这些是本地进程退出证据，不代表远端模型停止计算或计费。

全量回归 **294/294** 通过；最终 Job Object 调整后专项 **17/17** 通过，覆盖取消时仍存活的后代 PID 清理。堆内存与隔离无头入口校验通过。未提交、推送、发布或部署；Antigravity 路径继续留空。真实权限、CLI→Pi MCP、生产升级、全新机器/重启和远端 CI 仍需各自证据，崩溃日志仍由人工核对。

## 2026-09-20 — Faster, observable sequential calls / 连续调用加速与实时状态

Primary local pre-review, implementation, tests and post-review; no independent model verdict is claimed. The scope is the existing primary CLI runner, its wrappers, regression coverage and bilingual documentation. Added a session-local digest cache (32 entries, 60-second TTL) alongside the existing help cache (16 entries, 60 seconds). Exact version probes still run on every call; real path, file identity, size and nanosecond modification/change timestamps invalidate cached data. Read-time identity drift is rejected. This optimization is not an integrity boundary against a hostile filesystem owner. Model processes, conversations and host-owned authentication are not reused.

`HeadlessBatchEvents` / `batch-events` streams per-request phases, first output, metadata-only activity, periodic waiting status and completed results, followed by a full report. Waiting is local liveness metadata, not evidence of model generation. Batch requests are validated together before any execution; admission snapshots inputs, serializes library calls and stops later work after a failure. Cancellation/observer failures preserve earlier batch results and report unstarted requests. Final JSON without a newline now contributes response timing. Timings and file-read/cache counters separate queueing, preflight and CLI execution. Claude's final-JSON protocol remains unchanged; no partial-token streaming is claimed.

Verification: **302/302 Node tests passed**, zero skipped (`.test/sequential-full-suite.txt`, 242352 ms). This includes exact-version checks despite warm caches, expiry, restored-mtime content drift, explicit cache clearing, concurrent admission without overlapping CLI work, snapshot isolation, fail-stop behavior, entire-batch rejection before any CLI launch, sanitized phase events, silence heartbeats, mid-batch cancellation, callback failure, no-newline results, and the actual NDJSON CLI entry. Existing Windows native/Job Object cleanup and upgrade fixtures also passed. Resource regression passed (`.test/sequential-heap.json`): heap growth 2225624 bytes, RSS growth 25182208 bytes, active handles -3. This resource test is synthetic, not a real model benchmark.

Two model-free local comparisons used the previous development package (`.test/repair-build/pi-kether-portable-0.11.0.zip`) as the baseline. Each round used 24 fake-CLI invocations in ABBA blocks, excluded each block's initial cold call, and compared ten warm calls per implementation with the same executable, prompt and workspace. Preflight medians were **210.5 → 158 ms** in the first round and **255 → 175 ms** in the final round (about 25–31% lower). Total-call medians were 354.5 → 298 ms and 414.5 → 330.5 ms. Host timing varied; these observations do not establish real model response speed or other-machine performance. Each current warm call performed eight digest-cache hits and zero digest file reads; the executable was 86989128 bytes. Baseline digest counters were not instrumented, represented as null in the corrected final report. Evidence: `.test/sequential-benchmark.json`, `.test/sequential-benchmark-final.json`, reproducible local driver `.test/sequential-benchmark.mjs`. No model calls or credential reads were involved.

Both the PowerShell workflow entry and isolated packaged runner passed model-free batch-stream checks (`install/Test-Headless.ps1`), including inert defaults and installed-file drift rejection. Graph refresh reparsed two tracked files, reused 137 and reported no parse errors. Untracked modules remain outside the graph; two existing knowledge entries remain needs-review, with no automatic promotion or Git staging. Development packaging targets `.test/sequential-build`, with its actual build outcome in `.test/sequential-build.log`; published v0.11.0 attachments remain separate. No production Pi update, host policy change, commit, push or release is part of this change. Antigravity stays disabled with an empty path. Real-provider performance, independent model review, production deployment and remote CI are unverified for this revision.

### 简体中文

本轮由主代理完成本地前审、实施与复核，不宣称独立模型审查通过。新增最多 32 项、60 秒有效的文件摘要缓存，保留每次完整版本检查及文件身份/大小/高精度时间核对；变化或过期重新读取。批量入口 `HeadlessBatchEvents` 实时显示阶段、等待状态、各项结果和耗时汇总。等待信号来自运行器，不代表模型正在生成；进度不暴露提示、工具参数和原始错误。整批先校验，库会话固定提交输入、串行执行并在失败后阻止后续调用；不复用模型进程、对话或认证。补齐无末尾换行的响应统计。

完整回归 **302/302**、资源回归及隔离副本/PowerShell 批次入口验证通过。两轮模型无关对照中，暖调用预检中位耗时分别从 **210.5 降至 158 ms**、**255 降至 175 ms**，约减少 **25–31%**；当前每次暖调用命中 8 项摘要缓存，摘要读取量为 0。旧版缺少相应计数，最终报告以 null 表示未知。数据来自本机临时 CLI 夹具，不能推断真实模型生成速度或其他机器表现。关系索引更新 2 项、复用 137 项且无解析错误；未跟踪文件未自动暂存，两条知识仍待复核。开发包与发布附件分别保留，构建结果见 `.test/sequential-build.log`。未部署实际 Pi、修改认证、提交、推送或发布；Antigravity 继续留空禁用。

## 2026-09-20 — Authorized local Pi upgrade / 已授权的本机 Pi 升级

The user separately authorized deployment after the preceding source-only work. Local installation `C:/Users/asus/plugins/pi-dispatch` had no managed baseline and returned 404 for the new maintenance endpoint. It also contains host-owned authentication/editor adaptations, so the full portable tree was not substituted. A scoped deployment added the six headless fingerprint files, change-impact CLI and two bilingual guides, updated the code-graph module and merged only the three maintenance additions into the installed gateway: **11 files total**. Nineteen preserved configuration/auth/provider/editor/sandbox-related files retained their hashes; provider/authentication/LSP/root/access/governance/editor/knowledge capabilities compared equal before and after. No credential migration, dependency installation, WSL copy replacement or global-policy change was performed. No standard managed baseline was fabricated.

Pre-deployment integration used an isolated copy of the actual installed host code. Headless/acceptance tests passed **28/28**. The first gateway test copy lacked editor dependencies and package metadata; after restoring them, one fixture still referred to the portable `check_claude_auth` name. The fixture was adapted to the installed `renew_claude_auth` and injected `renewAuthFn`, preserving the assertion and making no real authentication call. Gateway lifecycle tests then passed **13/13** (`.test/local-continuous-gateway-accepted.txt`). Primary local pre/post review is the review evidence; no independent model review is claimed.

Backup hashes and a prepared journal were written before stopping the idle, identity-checked legacy process. The initial replacement passed health/readiness, Tunnel readiness and installed feature checks, but the legacy startup helper retained the upgrade command's output channel. A second restart used the newly installed maintenance endpoint, confirmed resource disposal and old-process exit, then spawned the same gateway detached with ignored stdio. This released the upgrade command normally. Final gateway PID: **93556**, following 75268 → 64040 → 93556; process identifiers are historical observations, not permanent configuration.

Actual installed checks passed two sequential model-free CLI fixture calls, immediate per-request results, waiting metadata, one help-cache hit and zero digest bytes read on the warm call. All six headless fingerprint files matched source. Authenticated HTTP MCP passed fresh graph status (139 tracked files), knowledge review (five records) and installed change-impact execution without modifying the project index. After the detached restart, a real Python LSP diagnostics request passed with `executionMode:direct`, `modelCalls:0`, `backend:lsp`, published empty diagnostics and successful server/sandbox cleanup (5274 ms). The gateway remained ready and idle with unchanged capabilities after the launcher exited. These checks do not establish current paid model access, real CLI model response performance, all-language LSP coverage or reboot persistence; optional clients remain disabled in the template, including the empty Antigravity path.

Local evidence: `.test/local-continuous-deployment.json`, `.test/local-continuous-live-check.json`, `.test/local-continuous-parity.json`, `.test/local-continuous-detached.json`, `.test/local-continuous-final-probes.json`. Backup/recovery journal: `C:/Users/asus/.local/state/pi-kether/deploy-backups/continuous-calls-20260920-01`. No commit, push or new release was performed. Existing development ZIPs represent their earlier packaged documentation; this deployment record does not retroactively modify them.

### 简体中文

用户另行授权后，已将连续调用等改动定向升级到实际 Pi。旧安装无受管基线/维护接口且包含本机专有认证与编辑器适配，因此仅更新 **11 个文件**，网关只合并维护代码；19 个保留文件的哈希、模型路由、认证、LSP、访问范围与治理能力均核对一致。先创建校验过的备份与日志，再在空闲状态确认进程身份并重启。隔离副本的连续调用 **28/28**、按本机认证接口调整后的生命周期测试 **13/13** 通过；未执行真实认证续期或独立模型审查。

首次启动后发现旧启动器仍占用升级命令的输出通道，随后通过新维护接口优雅关闭并以独立后台方式启动，升级命令已正常退出，最终进程为 **93556**。实际安装验证通过批次执行、逐项结果、等待状态、缓存命中、六个功能文件指纹一致、关系/知识查询及变更影响。最终真实 Python LSP 诊断 **success**，模型调用为 0、诊断已发布、服务器和沙箱清理成功；耗时 5274 ms。健康和 Tunnel 就绪，启动器退出后服务仍存活。保留回滚备份，未改认证、依赖、WSL 或全局规则，未推送发布；真实模型访问、全语言覆盖和重启持久性不在本次证明范围内。
## 2026-09-20 — Coordinator-only primary and accepted Luna implementation / 主代理协调与 Luna 编码验收

The user requested Astra as the reasoning/scheduling center and Pi Luna workers as implementation owners, and explicitly authorized sending the necessary workflow/process-tail source and existing patch to OpenAI Luna. Global host instructions, repository templates, both routing/governance skills and the generated workflow catalog now prohibit primary-authored code, tests and implementation scripts, including small fixes and fallback after worker failure. Policy and explanatory documents were authored by the primary; all accepted runtime code and test changes came from successful Luna/max workers. The primary inspected scope/base, mechanically integrated artifacts, ran existing verification commands and performed local pre/post review. Independent model review and a runtime-attested linked stage chain were not performed.

Accepted request suffixes `04`, `06`, `08` and `10` respectively delivered workflow policy metadata/tests, WSL model-tool isolation, updated exact tool-set assertions and a real sandbox-written editor-boundary test. Full request IDs and results are retained in `.test/coordinator-worker-04.json`, `-06.json`, `-08.json` and `-10.json`. Earlier attempts remain failures or cancellations: `01` was cancelled while queued with zero execution; `02` exited 143 without a final result; `03` timed out after generation; `05` timed out during generation; `07` returned unverified rather than a completed artifact; `09` exited normally but had one tool error and was rejected. Their patches were not accepted as successful delivery. Status reads and pre-dispatch validation failures are not model runs.

WSL model tasks no longer automatically load the legacy `pi-lsp-extension` or advertise its legacy tools. The controlled `yhwh_lsp_*` tools, read/write guards, authorized editor tool and existing direct deterministic/host compatibility paths remain. The legacy extension's automatic file-tool lifecycle was identified as a plausible contributor to process-tail delay; this is not proof that it caused every previous timeout. Only the two worker-authored runtime changes were mechanically applied to the installed `dispatch.mjs` and `provider-policy.mjs`, preserving host-specific authentication/provider adaptations. Backups and hashes are in `.test/coordinator-runtime-host-backup/` and `.test/coordinator-runtime-deployment.json`. A graceful detached restart passed health/readiness and disposal, moving the observed PID from 93556 to 91564 (`.test/coordinator-runtime-restart.json`).

The final live request `yhwh-live-worker-editor-test-clean-20260920-10` used `openai-codex / gpt-5.6-luna / max`, workspace-write with one allowed test file, and exactly one successful relative-path write. Execution was **39910 ms**, process tail **3186 ms**, cleanup **256 ms**, tool errors **0**; exit, role, format and cleanup validation passed. Its sole new-file patch was inspected and applied unchanged. The preceding failed request `09` also exited with a roughly 3-second tail but is not counted as success. These observations show the previous long tail did not recur in these two calls, not a general performance guarantee. Final gateway inspection showed accepting=true, active=0 and queued=0.

Final focused regressions passed **45/45**, zero skipped (`.test/coordinator-final-focused.log`); host profile/MCP fixtures passed **14/14** (`.test/coordinator-host-profiles.log`). Workflow catalog synchronization/check and whitespace validation passed. The tracked-file graph refresh parsed 5 files and reused 143 with no errors; new untracked tests remain outside that index, and existing knowledge entries were not automatically promoted. The full suite and remote CI were not rerun; previous remote failures are not resolved by these focused results. This is an instruction/audit policy, not an OS-level prohibition on all host editing tools. Global Codex policy is active; portable workflow metadata is updated in source, while the installed legacy gateway still lacks the newer workflow-discovery endpoint. No full multi-client deployment parity is claimed. No commit, push, package publication or release was performed.

### 简体中文

用户要求 Astra 只负责思考、拆分、调度与验收，并授权将本次必要源码和补丁发给 OpenAI Luna。已同步全局规则、仓库模板、治理/路由技能及工作流目录，关闭“小修复由主代理完成”和“worker 失败后主代理接管编码”的例外。主代理编写政策与说明、机械合入补丁并运行已有检查；实际源码与测试均来自成功交付的 Luna/max。审查为主代理本地前审/复核，不宣称独立 reviewer 或完整运行时阶段链通过。

本轮接受 `04/06/08/10` 四项交付；其余排队取消、退出异常、超时、未完成交付和工具错误分别保留原状态，没有把失败补丁算作成功。WSL 模型任务停止自动加载旧 LSP 扩展及其工具，保留受控 LSP、读写范围限制和授权编辑器能力。两处运行时代码已定向部署到本机 Pi，保留本机认证/模型适配与回滚备份，优雅重启后健康、就绪正常。

最终真实 Luna 写入任务 **39.91 秒**完成，工具错误为 **0**，进程收尾 **3.19 秒**，格式、角色及清理均通过；其测试补丁已原样合入。专项回归 **45/45**、宿主配置/MCP 夹具 **14/14**通过，工作流目录与空白检查通过。两次部署后调用均未再出现之前的长时间退出滞留，但不据此推断一般性能。规则属于指令与审计约束，不能技术上禁用所有客户端自身的编辑工具。未重跑全量测试或远端 CI，未宣称旧 CI 问题已解决；未提交、推送或发布。
## 2026-09-21 — Model-free asynchronous task heartbeats / 无模型异步任务心跳

Added `task-heartbeat.js` and integrated it into the existing task monitor. Each asynchronous submitted task creates one local supervisor heartbeat, starts it at running, records progress separately, and stops/freezes it on cancellation or every terminal path. Public task records expose heartbeat metadata through existing status/list tools. Default intervals are 10 seconds with a stale threshold greater than 30 seconds; queued tasks do not start a timer, reads do not generate beats, replay does not duplicate timers, and timers are unref'ed. This is local gateway-supervisor evidence with `modelCalls:0`, not WSL-process or remote-provider health. No automatic cancellation, retry, probing, completion-event subscription or primary-model wakeup was added. Existing execution deadlines and result acceptance remain unchanged.

All implementation and tests were authored by Pi `openai-codex / gpt-5.6-luna / max`; the primary performed host planning, local pre/post review, documentation, mechanical patch integration and existing test commands. Independent model review and a linked runtime stage chain are not claimed. Six task attempts under `yhwh-task-heartbeat-20260921` were retained honestly: core-01 timed out at 300 seconds, helper-02 succeeded (163188 ms), wire-03 exited 143 without a final response after out-of-scope LSP calls despite zero reported tool errors, artifact-04 timed out at 300 seconds, monitor-delivery-05 succeeded (54143 ms), and tests-delivery-06 succeeded (106700 ms). No failed delivery was accepted directly. The existing proposal was subsequently inspected and successfully redelivered by Luna as separate file artifacts before integration. The host did not author repair code or relax result validation. Raw results/patches are in `.test/heartbeat-*-result.json`; successful file artifacts are preserved in those results.

Baseline existing regression **47/47**, integrated existing regression **47/47**, and final combined heartbeat/monitor/gateway/UI regression **55/55** passed with zero skipped (`.test/heartbeat-baseline.log`, `.test/heartbeat-integrated-existing.log`, `.test/heartbeat-final.log`). New deterministic tests use an injected clock/timers rather than real model calls or 30-second waits. They cover freshness/staleness, isolated progress, read-only snapshots, late-callback freeze, queued/running cancellation, success/blocked/failure/throw cleanup, timer unref and replay deduplication. Graph refresh parsed one tracked module, reused 147, and reported no errors; new untracked helper/tests remain outside its tracked-file coverage. Existing knowledge was reviewed without automatic promotion. README language buttons were preserved and heartbeat documentation supplied in both languages.

This verifies source behavior and local fixtures only. The installed running Pi service was not upgraded or restarted; no heartbeat badge was added to the UI, no real remote-provider liveness was established, and no full-suite/CI result or measured token saving is claimed. No commit, push, package publication or release occurred.

### 简体中文

新增异步任务的本地监管心跳：开始执行后每 10 秒更新，超过 30 秒未更新则读取为过期；排队不启计时器，读取不刷新心跳，取消/终态立即停止并冻结快照，重放不重复创建计时器。执行层进度单独计时，静默不触发取消或重试。心跳本身模型调用为 0，但只说明网关监管回调运行过，不证明子进程或远端模型健康，也没有新增完成事件订阅或自动唤醒主代理。

源码和测试均由 Luna/max 编写，主代理仅本地前审/复核、机械合入、文档和验证。六次派发中三次成功、三次失败；失败分别为两次生成超时及一次缺少完整结果的异常退出。失败补丁没有直接算作成功，而是经 Luna 重新核对并以小文件交付后才合入。没有独立模型审查或完整阶段链声明。最终组合回归 **55/55** 通过，无跳过；关系索引增量更新 1 项、复用 147 项且无错误，新未跟踪文件不在索引中。双语文档已补齐。尚未升级运行中的 Pi、添加界面心跳标识、推送或发布，未声称全量/CI 通过或已测得 token 节省。
## 2026-09-21 — Authorized local heartbeat deployment and strict no-polling / 已授权本机心跳部署与严格禁止轮询

Following explicit user authorization, installed only `extensions/task-heartbeat.js` and `extensions/task-monitor.js` into `C:/Users/asus/plugins/pi-dispatch`. The installed monitor exactly matched the pre-change baseline before replacement. Backed up the original module, global policies and preceding restart record; retained before/after hashes. Tests against an isolated copy of the actual installed extensions passed **10/10**. The installed gateway, dispatch, provider policy and Claude renewal source hashes remained unchanged. The existing restart utility confirmed gateway process identity, graceful disposal, old-process exit and detached startup; observed PID changed **34852 → 16596**, health and readiness passed. Final loopback listener belonged to PID 16596, and native plus connector MCP capability reads succeeded. Worker/reviewer role bindings compared equal; the new instance was accepting with zero active/queued tasks.

The user also explicitly strengthened the waiting policy. Global AGENTS, coordinator-only and async-results references, repository templates, bilingual documentation and workflow catalog now prohibit Astra's repeated polling, including sleep/query loops, 50/60-second batches and script-wrapped status loops. Earlier tool-side polling/fallback permission is revoked. Available real completion waits remain preferred; otherwise no status query until a genuine signal, predeclared task deadline or explicit user request. Finite acceptance reads and full-result pagination are preserved. This is an instruction policy, not removal of MCP tools or a new completion-notification transport. Current Codex context reloaded the updated global rule.

Catalog synchronization and existing policy/MCP tests passed **5/5**. Deployment involved **zero model calls** and reused existing worker-authored code/tests and operational tools; no new implementation was authored by the primary. Review was local host pre/post review, not an independent model verdict. Evidence and rollback files: `.test/heartbeat-deploy-20260921/`, including `installed-fixture-tests.log`, `policy-tests.log`, `restart.json`, file hashes and `acceptance.json`. No live model task was launched solely to measure heartbeat ticks; the deployed logic is established by installed-copy tests, byte parity and fresh-process startup, not a new remote-provider health probe. No commit, push or release occurred.

### 简体中文

用户明确授权后，仅更新本机 Pi 的心跳组件与任务监控器两个文件。部署前确认旧文件基线一致并备份；已安装依赖的隔离副本测试 **10/10** 通过。保留网关、派发器、模型策略与 Claude 续期源码哈希，复用现有工具优雅重启，进程 **34852 → 16596**；本机端口、健康/就绪、原生及连接器 MCP 检查通过，角色绑定一致，服务空闲可接单。

全局规则及仓库模板已明确禁止 Astra 反复轮询，连同“等待后查询”、每 50/60 秒批量查询及脚本包装轮询一并禁止，撤销旧版定时兜底许可。仅在真实事件、预定截止时间、用户询问或有限验收时读取必要状态，完整结果分页保留。新 AGENTS 已进入当前上下文，策略/MCP 回归 **5/5** 通过。本次部署 **模型调用为 0**，未为测量心跳额外发起模型任务；这是规则约束与运行部署，不是新增主动唤醒接口。备份和证据见 `.test/heartbeat-deploy-20260921/`，未提交、推送或发布。
## 2026-09-21 — Authorized GitHub synchronization / 已授权 GitHub 同步

The user authorized pushing the accumulated coordinator-only policy, WSL model-tool isolation, regression coverage, strict no-polling rules and deterministic task heartbeats to the existing private `Yuzu482/YHWH` repository on `main`. The branch matched fetched `origin/main` before staging. The explicit scope contains 29 files; local `.test` logs/backups and host credentials are excluded. Prior source regression results remain 45/45 (coordinator/routing), 51/51 (decomposition demonstration) and 55/55 (heartbeat/gateway); these suites overlap and must not be summed as unique tests. Deployment-copy checks passed 10/10 and updated workflow-policy checks 5/5. Catalog synchronization and staged whitespace checks passed. No full-suite or new remote CI success is claimed.

After the authorized Git add, code-graph refresh included the six new code/test files: 6 parsed, 148 reused, 154 total, no parse errors. Earlier entries describing those files as untracked refer to their pre-staging state. Project knowledge was inspected but not automatically promoted. This is source synchronization only, with no new version, tag or release.

用户授权将累计的主代理协调规则、WSL 工具隔离、回归测试、严格禁止轮询及确定性心跳推送到私有仓库 `Yuzu482/YHWH` 的 `main`。提交范围为明确列出的 29 个文件，不包含本地日志、备份或认证数据。既有专项测试结果均保留，重叠用例不累加为独立数量；工作流目录与暂存差异检查通过，未宣称全量或新远端 CI 通过。正式暂存后，关系索引新增解析 6 项、复用 148 项，共 154 项且无错误。此前“未跟踪”描述是暂存前记录。本次不创建版本标签或 release。

## 2026-09-21 — v0.12.0 release preparation / 发布准备

The user authorized a new private GitHub release covering all changes since v0.11.0: `bee3e54` and `3c65085`. Version metadata was authored by Pi `openai-codex / gpt-5.6-luna / max`, request `yhwh-release-012-version-20260921`, and mechanically integrated after successful execution, format/role validation, cleanup and exact diff review. It changed only `portable.manifest.json` from 0.11.0 to 0.12.0. Primary-authored release documentation and README download examples were updated in both languages. Pre/post review is local primary review, not independent review or a linked runtime stage chain. No implementation logic changed in this preparation.

Fresh local full Node regression passed **320/320**, zero failures/skips, in **164.80 s** (`.test/release-012-node.log`). Heap regression passed (`.test/release-012-heap.log`): 25 warmups, 400 ordinary requests, and 40 each large-output/cancelled/disconnected cases. Project-knowledge review reported no format errors; entries were not automatically refreshed or promoted. The subsequent package build uses `-SkipTests` only because this same runtime source just passed the full Node and heap checks; installer/configuration, encryption, license, plugin, archive and generated-installer checks still run. Final artifact verification is recorded in the GitHub release validation addendum.

Baseline GitHub CI run 35531780910 failed seven tests: two missing installed Pi/LSP dependencies and five authentication-renewal persistence/cross-process fixtures. Local passing tests do not resolve or override this CI evidence. Antigravity live acceptance, fresh-machine installation, reboot persistence, full multi-client governance and the legacy LSP public-redistribution notice remain incomplete. Publishing does not deploy or restart any local service.

用户授权创建 v0.12.0 私有 release，收录 v0.11.0 后两次功能提交。版本元数据由 Pi Luna/max 成功交付，主代理核对格式、角色、清理与变更范围后机械合入；双语发布说明与 README 下载示例同步更新，前后审查为主代理本地审查，未宣称独立审查或完整阶段链。本次准备不修改运行逻辑。

新一轮本机全量 Node 回归 **320/320** 通过，零失败、零跳过，耗时 **164.80 秒**；内存回归通过。项目知识检查无格式错误，未自动刷新或确认条目。后续构建仅因同一运行源码已通过上述全量和内存测试而使用 `-SkipTests`，安装、配置、加密、许可、插件、归档和生成安装器检查仍执行；最终附件验收记于 GitHub release 的验证补充。基线远端 CI 的 7 项失败仍保留，不以本机通过代替 CI 通过。发布不自动部署或重启本机服务。

## 2026-09-22 — v0.13.0 release preparation / 发布准备

The user authorized pushing the current completed workflow changes to the existing private Yuzu482/YHWH repository and creating a new release. The target is main and v0.13.0. This includes native worker effort/default and execution-budget changes, metadata-only stream diagnostics, and previously accepted Windows execution-policy plus credential-free Pi/LSP test-fixture fixes. Unfinished completion waits and delivery-template drafts remain outside the payload.

The implementation source passed the isolated full suite **326/326**, with zero failures or skips, in 306.811 seconds; temporary USERPROFILE/HOME and a restricted executable PATH were used. Focused budget/dispatch/role/API checks passed **34/34** and are a subset. The heap/handle regression passed again during release preparation. Catalog consistency, configuration validation, portable self-test and diff whitespace checks passed. Source has not changed since the full suite except for the single package-version value and documentation/index updates. Build-Release uses -SkipTests to reuse this exact-source evidence; package, configuration, encryption, license, plugin and generated-installer checks still run. Final archive hashes, package check results and exact-commit GitHub CI status are recorded in the Release verification addendum, not presumed from this preparation record.

Code authorship: the accepted effort patch came from Pi Luna/high, request yhwh-effort-budget-20260922-04, with successful terminal execution, valid v2 role output and successful cleanup. Earlier failed/unverified proposals were not accepted. The primary normalized only patch transport/context metadata, preserving every source addition/deletion. Version metadata came from Pi Luna/low, request yhwh-release-013-version-20260922, and only changed portable.manifest.json version 0.12.0 to 0.13.0. Review is local primary pre/post review; independent Claude review remains unavailable because of authentication. No linked full-stage attestation or matched-task effort benchmark is claimed.

No local service upgrade or restart is included in publication. Installed native workers can receive explicit effort selections under updated host policy, but service defaults and new execution-budget code require a separate upgrade. Reviewer and controlled API bindings remain unchanged. A private release does not resolve the documented legacy pi-lsp-extension upstream notice limitation or prove fresh-machine installation, reboot persistence, all-client behavior, or token savings.

用户授权把当前已完成改动推送到原私有仓库 Yuzu482/YHWH 的 main 并创建 v0.13.0。范围包含原生 worker 思考档位与交付预算、执行流元数据诊断，以及此前已验收的 Windows 兼容性和无凭据测试夹具修复；未完成的事件等待和交付模板草稿不进入发布包。

相同实现源码隔离全量回归 **326/326** 通过，无失败或跳过，耗时 306.811 秒；专项 **34/34** 属于其子集。发布准备阶段重新运行堆／句柄内存回归并通过。目录一致性、配置、便携自检及差异空白检查通过。全量测试后仅修改包版本元数据、文档和索引；构建使用 -SkipTests 复用该源码证据，但继续执行安装、配置、加密、许可、插件与生成安装器检查。最终附件哈希、构建结果和准确提交的 GitHub CI 状态记录于 Release 验证补充，不由本段预先推断。

思考与预算源码由 Luna/high 的 04 号任务成功交付，版本元数据由 Luna/low 的 yhwh-release-013-version-20260922 成功交付；主代理仅做机械整合和本地前后审查。Claude 独立审查因认证问题未完成；不声称完整阶段链或档位性能对照实验通过。发布不升级或重启本机服务，不改变 reviewer／API 路由绑定，不消除旧 LSP 上游通知限制，也不证明新机安装、重启持久性、全部客户端体验或 token 节省。
